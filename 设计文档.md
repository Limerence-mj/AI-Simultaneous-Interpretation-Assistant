# AI 同声传译助手 · 需求设计文档

| 版本 | 日期 | 作者 | 修订说明 |
|------|------|------|----------|
| v2.0 | 2026-06-05 | - | 完整功能规格与实现步骤划分 |
| v2.1 | 2026-06-05 | - | 补充附录、TTS、测试策略、日志、降级等 |

---

## 目录

1. [项目概述](#1-项目概述)
2. [用户与场景](#2-用户与场景)
3. [功能需求详述](#3-功能需求详述)
4. [非功能需求](#4-非功能需求)
5. [界面与交互设计](#5-界面与交互设计)
6. [技术方案详述](#6-技术方案详述)
7. [实现步骤与里程碑](#7-实现步骤与里程碑)
8. [测试策略](#8-测试策略)
9. [验收标准](#9-验收标准)
10. [风险与应急预案](#10-风险与应急预案)
11. [附录](#11-附录)

---

## 1. 项目概述

### 1.1 项目背景

用户经常需要观看英文演讲、技术分享、国际会议或网课，但受限于英语听力水平，难以实时跟上内容节奏。现有方案要么依赖人工同传（成本高、不可复用），要么需要等待事后字幕（时效性差）。

### 1.2 竞品简析

| 方案 | 优点 | 缺点 |
|------|------|------|
| 同传人工翻译 | 准确、自然、适应语境 | 昂贵（每小时数千元）、不可复用 |
| YouTube 自动字幕+翻译 | 免费、平台集成 | 延迟大、需联网、不适用于会议 |
| 讯飞听见 / 腾讯同传 | 识别准、延迟低 | 付费服务、需联网、隐私风险 |
| OBS + 实时字幕插件 | 开源免费 | 配置复杂、无翻译、仅转写 |
| **本项目** | **免费、完全离线、隐私安全、自修正** | 模型精度受限于本地算力、需手动下载模型 |

### 1.3 产品定义

一款**完全离线运行**的桌面端AI同声传译助手，能够**实时捕获系统音频或麦克风输入**，将英文语音流**逐句识别并翻译成中文**，以**可拖拽、半透明悬浮字幕**形式呈现在屏幕前端，同时支持**可选的语音合成播报**，并具备**基于上下文的自动修正能力**，帮助用户跨越语言障碍，实时理解内容。

### 1.4 核心约束

- **零云端依赖**：所有模型本地运行，不调用任何付费/联网 API。
- **模型全开源**：使用 MIT 许可的 Whisper、Apache 2.0 的翻译模型等。
- **单人 3 天可交付**：功能聚焦，管线清晰，可演示完整闭环。
- **所有产出物存放于项目根目录**：模型、配置、日志、导出文件均不写入 C 盘或其他系统目录。

### 1.5 核心价值

- **实时性**：端到端延迟控制在 3~5 秒（GPU）或 5~8 秒（CPU），跟上说话人节奏。
- **隐私性**：完全离线，音频不留出本机。
- **自修正**：随着上下文积累，自动纠正早期的代词、名词翻译错误。
- **开箱即用**：下载可执行文件即可使用，无需配置环境。
- **语音播报**（可选）：支持将翻译结果以语音形式朗读，满足"听译"场景。

---

## 2. 用户与场景

### 2.1 目标用户画像

| 用户类型 | 典型特征 | 核心需求 |
|----------|----------|----------|
| 技术学习者 | 观看英文技术演讲、教程 | 实时翻译专业术语，跟上思路 |
| 职场人士 | 参加跨国线上会议 | 不遗漏讨论要点，事后可复盘 |
| 英语学习者 | 听力练习中需要辅助理解 | 中英对照，理解真实语速内容 |
| 内容创作者 | 整理外文视频素材 | 快速获取译文文本，提高效率 |

### 2.2 典型使用场景

| 场景编号 | 场景名称 | 音频来源 | 用户操作 |
|----------|----------|----------|----------|
| S1 | 观看在线视频 | 浏览器/播放器音频 | 打开工具，选择系统音频，开始翻译，观看视频同时阅读字幕 |
| S2 | 参与实时会议 | Zoom/Teams 音频 | 打开工具，选择系统音频，专注会议内容 |
| S3 | 线下会议/课堂 | 麦克风拾取环境音 | 将麦克风靠近音源，选择麦克风输入 |
| S4 | 翻译本地音视频文件 | 本地文件 | 导入文件，逐句处理并生成字幕 |
| S5 | 听译模式 | 任意音频源 | 开启语音播报，通过耳机收听中文翻译 |

### 2.3 用户使用流程

```
启动应用 → 选择音频源 → 点击[开始] → 字幕窗口出现
                                      ↓
                              音频流实时处理
                                (ASR → MT)
                                      ↓
                              字幕实时刷新(含修正)
                              语音播报(可选TTS)
                                      ↓
                              点击[停止] → 可导出字幕
```

---

## 3. 功能需求详述

### 3.1 功能清单与优先级

| 编号 | 功能模块 | 功能名称 | 优先级 | 说明 |
|------|----------|----------|--------|------|
| F1 | 音频输入 | 系统音频捕获 | P0 | 捕获 Windows/macOS 系统播放声音 |
| F2 | 音频输入 | 麦克风音频捕获 | P1 | 捕获物理麦克风输入 |
| F3 | 音频输入 | 本地音频文件导入 | P2 | 处理已录制文件（.wav/.mp3/.m4a） |
| F4 | 语音识别 | 实时语音转写 | P0 | 使用 Whisper 将语音转为英文文本 |
| F5 | 机器翻译 | 实时英译中 | P0 | 使用翻译模型将英文转为中文 |
| F6 | 字幕显示 | 悬浮字幕窗口 | P0 | 半透明、置顶、可拖拽字幕条 |
| F7 | 上下文修正 | 实时修正与回填 | P0 | 根据新上下文修正之前译文的错误 |
| F8 | 历史记录 | 对话记录回溯 | P1 | 显示原文/译文历史，可滚动查看 |
| F9 | 导出功能 | 字幕文件导出 | P2 | 导出为 TXT 或 SRT 格式 |
| F10 | 界面设置 | 字幕样式调节 | P1 | 字体大小、透明度调节 |
| F11 | 状态指示 | 运行状态与性能 | P1 | 显示当前状态、延迟、句数统计 |
| **F12** | **语音合成** | **TTS 语音播报** | **P2** | **将中文译文以语音朗读，支持开关和语速调节** |
| **F13** | **日志系统** | **运行日志记录** | **P1** | **关键事件与错误日志，便于排查问题** |

### 3.2 功能详细规格

#### F1-F3 音频输入模块

**F1 系统音频捕获**
- 输入：操作系统的音频输出设备（扬声器/虚拟声卡）
- 处理：使用 `sounddevice` 库或平台特定 API（Windows WASAPI Loopback，macOS BlackHole 虚拟设备）获取系统音频流
- 输出：PCM 16-bit, 16kHz 单声道音频流
- 异常处理：
  - 无可用设备 → 提示"未检测到音频设备，请检查音频设置"
  - 权限不足 → 提示"需要音频录制权限"
  - 音频流中断 → 自动尝试重连，最多3次

**F2 麦克风音频捕获**
- 输入：系统默认麦克风
- 输出：同上 PCM 音频流
- 设备选择：显示设备列表，允许用户选择

**F3 本地音频文件导入**
- 支持格式：.wav, .mp3, .m4a
- 处理：使用 `pydub` 或 `ffmpeg` 重采样为 16kHz 单声道
- 输出：模拟为音频流逐段送入管线
- 文件大小限制：建议 ≤500MB，超出提示性能风险
- 文件模式下支持：**暂停/恢复**、**进度条显示**（已处理百分比）、处理完成提示

#### F4 实时语音识别（ASR）

- 模型：`faster-whisper`，默认加载 `small` 模型（大小约 1.8GB），允许用户降级为 `tiny`（约 700MB）
- 引擎特性：
  - 基于语音活动检测（VAD）自动切句
  - 流式处理：边听边切，完整短句形成后立即送入 ASR
  - 支持热词表（可选）：可配置常见专业术语提升识别率（v1.0 不强制，但预留接口）
- 流程：
  1. 音频流经过 VAD 模块，检测语音段边界
  2. 当静音持续时间 ≥ 0.5 秒，视为一句话结束
  3. 将该语音段缓存为 WAV 片段，送入 ASR 队列
  4. ASR 线程依次处理，输出 {时间戳, 英文文本}
- 性能目标：
  - GPU (NVIDIA GTX 1060+)：单句识别延迟 < 1.5 秒
  - CPU (Intel i5 8代+)：单句识别延迟 < 4 秒
- 接口：`recognize(audio_chunk: bytes) -> dict{text, start, end}`
- **热词表预留接口**（v1.0 不实现，仅设计）：

```json
// hotwords.json (v1.0 预留，v2.0 启用)
{
  "enabled": false,
  "words": ["LangChain", "LoRA", "transformer"],
  "boost": 2.0
}
```

#### F5 实时机器翻译（MT）

- 模型：`Helsinki-NLP/opus-mt-en-zh`，通过 `transformers` 加载
- 模型大小：约 600MB
- 性能：CPU 单句翻译 < 200ms
- 工作模式：
  - 非流式句子级翻译：接收完整英文句子，返回中文
  - 支持上下文增强：翻译当前句子时，将前 N 句英文原文作为 prompt 前缀（Truncated context）
- 流程：
  1. 翻译队列收到英文句子及其历史上下文（前3句）
  2. 拼接上下文与当前句，形成输入 `"context1 context2 context3 current"`
  3. 调用模型生成中文，返回 `{zh_text, en_text, timestamp}`
- 异常处理：
  - 模型加载失败 → 提示"翻译模型损坏，请重新安装"
  - 翻译失败 → 降级显示英文原文，并标记错误
- 接口：`translate(en_text: str, context: List[str]) -> str`
- **多语言扩展预留**（v1.0 仅支持 en→zh）：
  - `language` 参数设计为 `(source_lang, target_lang)` 元组
  - 未来可选模型：`facebook/nllb-200-distilled-600M`（支持200种语言）、`SeamlessM4T`

#### F6 字幕悬浮窗口

- 窗口属性：
  - 无边框、半透明（默认背景透明度 60%）
  - 置顶（always on top）
  - 可拖拽移动
  - 默认位置：屏幕底部居中，占屏幕宽度 60%
- 显示内容：
  - 当前最新翻译中文，单行或两行显示
  - 字体：白色，无衬线，默认 24px，可调
  - 被修正时：轻微高亮闪烁（黄色渐变，持续1秒）表示内容更新
- 交互：
  - 右键菜单：调节字体大小（+/-）、透明度（+/-）、显示历史面板、退出
  - 双击：切换是否置顶
  - 可调整窗口宽度（拉伸两侧边缘）
- **多显示器支持**：
  - 默认在主显示器显示
  - 允许拖拽到任意显示器
  - 关闭时记忆窗口位置和所在显示器，下次启动恢复

#### F7 上下文修正机制

**修正目标**：解决代词指代不明、名词歧义、不完整句子误译等问题。

**修正类型**：

1. **断句重译（Reshoot）**
   - 场景：VAD 初期误切，如"I think the most important"被当作完整句，后发现后面还有"thing is timing"
   - 实现：ASR 后句与前句时间戳重叠或间隔 < 0.3 秒，则合并句子，重新翻译，用新结果替换旧显示。
   - 判定条件：若新识别片段与上一句在时间轴上几乎连续（间隔 < 300ms）且无明确句边界标点，则触发合并。

2. **上下文回顾翻译（Context-aware Re-translation）**
   - 场景：英文中 "it", "they", "Apple" 等需要上下文消歧
   - 实现：每翻译一个新句子，除了当前句，还将**之前已确认的 2~3 句英文原文**拼在 prompt 中（不拼翻译结果），让翻译模型基于上下文重新翻译当前句。如果新译文与之前首次译文不同，则更新字幕，并标记为"修正"。
   - 机制：
     - 维护一个滑动窗口：最近 4 句英文原文（当前句+前3句）
     - 翻译时，输入格式：`"prev1 prev2 prev3 <sep> current"`
     - 比较新旧译文：采用编辑距离（Levenshtein）>2 或关键实义词不同，则判定为需修正。
   - 历史记录中同时保留首次译和修正后译，显示样式：~~旧译文~~ → 新译文

**修正效果演示设计**：
- 在演示视频中，故意播放一段包含 "Apple announced a new chip. It is faster than the previous one." 的音频。
- 首次 "It is faster..." 可能译为 "它更快了..." ，上下文修正后变为 "新芯片比上一代更快"。

**用户控制**：
- 主面板提供「自动修正」开关，用户可选择关闭（偏好稳定输出时）

#### F8 历史记录面板

- 形式：从主面板展开或独立窗口，显示可滚动的原文-译文对照列表
- 每条记录包含：
  - 时间戳
  - 英文原文（如被合并则显示完整原文）
  - 中文译文（当前最终版本）
  - 若有修正，显示原始译文并划线
  - 修正标记高亮（黄色背景）
- 允许复制单条或全部内容
- 修正次数统计显示于面板底部

#### F9 导出功能

- 导出为 TXT：纯文本，每行一句中文
- 导出为 SRT：标准字幕格式，包含序号、时间轴、中文文本
- 时间轴来自 ASR 产生的时间戳
- 文件模式下支持**批量导出**（一次性生成完整 SRT）

#### F10 界面设置

- 字体大小：小(18px)/中(24px)/大(32px)，热键 Ctrl+[ / Ctrl+]
- 透明度：20%/40%/60%/80% 四档，热键 Ctrl+Shift+[ / ]
- 设置持久化：保存在本地 `config.json`（位于项目根目录），下次启动读取

#### F11 状态指示

- 主面板状态灯：
  - 🟢 绿色：正常工作
  - 🟡 黄色：处理中（模型加载、音频缓冲）
  - 🔴 红色：错误（无音频设备、模型缺失等）
- 实时统计：已翻译句数、平均延迟（毫秒）、修正次数
- 延迟定义：从原句说完到中文显示的时间差，取最近 5 句平均

#### **F12 语音合成播报（TTS）** — 新增

- 优先级：P2（v1.0 可降级，v2.0 实现）
- 功能开关：主面板提供「语音播报」复选框
- TTS 引擎选型：
  - 方案A（推荐）：`edge-tts` — 调用 Windows 系统内置语音，无需额外模型，延迟低
  - 方案B：`piper-tts` — 完全离线开源 TTS，需下载语音模型（~50MB）
  - 方案C：`pyttsx3` — 跨平台离线 TTS，使用系统 SAPI5（Win）/ NSSpeechSynthesizer（Mac）
- 交互参数：
  - 语速：0.8x / 1.0x / 1.2x / 1.5x 四档
  - 音量：独立于系统音量调节
- 工作模式：
  - 每句翻译完成后 → 送入 TTS 队列 → 按序朗读
  - 新句到达时若上一句仍在朗读 → 可配置「打断」或「排队」策略（默认打断）
- 异常处理：TTS 引擎不可用时，自动降级为纯字幕模式，不阻断翻译管线

#### **F13 日志系统** — 新增

- 优先级：P1
- 日志级别：DEBUG / INFO / WARNING / ERROR
- 输出方式：
  - 开发模式：控制台实时输出（INFO 及以上）
  - 生产模式：写入文件 `logs/app_{YYYY-MM-DD}.log`（位于项目根目录）
- 滚动策略：保留最近 7 天日志，自动清理
- 记录内容：
  - 模型加载耗时、内存占用
  - 每句 ASR/MT 延迟（DEBUG 级别）
  - 异常堆栈（ERROR 级别）
  - 设备状态变化（INFO 级别）
- **隐私保护**：日志**不记录**原始音频数据，翻译文本记录可选关闭

---

## 4. 非功能需求

### 4.1 性能

| 指标 | 目标值 | 条件 |
|------|--------|------|
| ASR 延迟 (单句) | < 1.5s (GPU) / < 4s (CPU) | small 模型 |
| MT 延迟 (单句) | < 200ms | CPU |
| TTS 延迟 (单句) | < 300ms | CPU，edge-tts |
| 端到端延迟 (说话结束到显示) | < 3s (GPU) / < 6s (CPU) | - |
| 内存占用 | < 2.5GB | 含模型 + 应用 |
| CPU 持续占用 | < 40% (i5) | 识别+翻译并行 |
| 模型加载时间 (首次) | < 30s (GPU) / < 60s (CPU) | - |
| 可处理最长音频 | 连续 2 小时 | 无内存泄漏 |

### 4.2 兼容性

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 1809+; macOS 12.0+ |
| CPU 架构 | x86_64 (必须); ARM64 (macOS Apple Silicon 通过 Rosetta2 兼容) |
| Python 版本 | 3.10+ (若提供脚本) |
| 交付形式 | 打包的独立可执行文件 (.exe / .app) + 模型文件 |

### 4.3 可用性

- 安装：下载解压即用，无需手动安装 Python 或 CUDA
- 首次启动检查模型文件完整性，缺失则提示下载（提供离线模型包）
- 界面无英文术语，全中文
- 最小化到系统托盘，托盘图标状态色同步
- **首次使用引导**：3 步引导（选设备 → 等模型加载 → 点击开始）

### 4.4 健壮性

- 音频设备热插拔：设备变化后自动检测，提示用户重新选择
- 模型推理异常：单次失败跳过该句，记录日志，不中断整体
- 窗口分辨率变化：自动调整字幕位置，保持在屏幕底部居中
- 长时运行：连续工作 2 小时无内存泄漏（通过循环测试验证）
- 多显示器连接/断开：字幕窗口自动跟随或保持在可见区域

### 4.5 安全性

- 完全离线运行，无网络通信
- 音频数据不落盘持久化（仅处理时暂存于内存）
- 日志文件不包含原始音频数据
- 导出文件默认保存于项目根目录 `exports/`，不写入系统敏感目录

---

## 5. 界面与交互设计

### 5.1 主控制面板

```
┌──────────────────────────────────────────┐
│  🎙️ AI 同声传译助手                 _ │
├──────────────────────────────────────────┤
│  音频源:    [系统音频 ▼]                │
│  设备:      [扬声器 (Loopback) ▼]      │
│  源语言:    [英语 ▼] (v2.0扩展)        │
│  字幕字体:  中(24)  [-] [+]            │
│  透明度:    ████████░░ 60%             │
│  ☑ 自动修正  ☐ 语音播报               │
│  语速:      [1.0x ▼]                   │
│                                          │
│  [ ▶ 开始翻译 ]  [ ⏹ 停止 ]           │
│                                          │
│  状态: 🟢 运行中 · 已运行 10:23        │
│  已翻译 89 句 · 修正 3 次 · 延迟 1.8s │
│  [ 📋 历史记录 ] [ 💾 导出 ]          │
└──────────────────────────────────────────┘
```

### 5.2 字幕窗口

```
┌──────────────────────────────────────────────┐
│                                              │
│  我认为最重要的因素是时机把握。              │
│                                              │
└──────────────────────────────────────────────┘
```

- 背景色：`#000000` 透明度 60% (可调)
- 文字色：`#FFFFFF`
- 圆角 12px，底部外边距 40px
- 默认宽度 800px，高度自适应文字（1-2行），最大高度 140px
- 显示动画：新内容淡入 (300ms ease-in)
- 修正动画：黄色渐变闪烁 (1s)，表示内容已被修正
- **位置记忆**：关闭时记住窗口位置与所在显示器，重启时恢复

### 5.3 历史记录窗口

```
┌──────────────────────────────────────────────────────────┐
│ 时间     │ 原文                      │ 译文              │
├──────────┼───────────────────────────┼───────────────────┤
│ 00:01.2  │ Hello everyone.           │ 大家好。          │
│ 00:03.5  │ Today I'll talk about AI. │ 今天我将讨论AI。  │
│ 00:06.8  │ It is changing the world. │ ~~它正在改变~~ →  │
│          │                           │ 人工智能正在改变   │
│          │                           │ 世界。  [修正×1]  │
└──────────────────────────────────────────────────────────┘
```

### 5.4 交互规范

- 快捷键：
  - `Ctrl+Shift+H`：显示/隐藏历史记录
  - `Ctrl+Shift+S`：停止/开始翻译
  - `Ctrl+Shift+E`：导出字幕
  - `Ctrl+Shift+V`：开关语音播报
- 所有操作非阻塞，不卡顿音频流

---

## 6. 技术方案详述

### 6.1 系统架构图

```
┌─────────────────────────────────────────────┐
│                   GUI Layer                  │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│   │主控制面板│  │悬浮字幕  │  │历史记录  │  │
│   └────┬─────┘  └────┬─────┘  └────┬─────┘  │
│        │             │             │        │
│   ┌────┴─────────────┴─────────────┴────┐   │
│   │        State Manager (单例)         │   │
│   │   - 当前译文   - 历史记录列表      │   │
│   │   - 运行状态   - 配置项            │   │
│   └────────────────┬───────────────────┘   │
└────────────────────┼───────────────────────┘
                     │
┌────────────────────┼───────────────────────┐
│              Core Pipeline                  │
│   ┌────────────┐   ┌──────────┐            │
│   │ Audio      │──▶│ VAD      │            │
│   │ Capturer   │   │ Silero   │            │
│   └────────────┘   └────┬─────┘            │
│                         │ speech segments  │
│                         ▼                  │
│              ┌──────────────────┐          │
│              │   ASR Engine     │          │
│              │ faster-whisper   │          │
│              └────────┬─────────┘          │
│                       │ en text            │
│                       ▼                    │
│              ┌──────────────────┐          │
│              │   MT Engine      │          │
│              │ opus-mt-en-zh    │          │
│              └────────┬─────────┘          │
│                       │ zh text            │
│              ┌────────▼─────────┐          │
│              │ Correction &     │          │
│              │ Update Logic     │          │
│              └────────┬─────────┘          │
│                       │                    │
│              ┌────────▼─────────┐          │
│              │ TTS Engine       │ (可选)   │
│              │ edge-tts/pyttsx3 │          │
│              └──────────────────┘          │
│                     │                      │
└─────────────────────┼──────────────────────┘
                      │ events
                      ▼
               State Manager (更新UI)
```

### 6.2 模块详细设计

#### 6.2.1 音频捕获模块 (audio_capture.py)

- 类：`AudioCapture`
- 方法：
  - `list_devices() -> List[Device]`: 枚举音频设备
  - `start(device_id, callback)`: 开始捕获，回调接收 `numpy array` 音频块
  - `stop()`: 停止捕获
- 内部使用 `sounddevice.InputStream`，设置 `blocksize=1024, samplerate=16000, channels=1`
- 系统音频环路（Windows）：通过 WASAPI loopback 设备，需要指定设备 ID
- 系统音频环路（macOS）：依赖 BlackHole 虚拟驱动，需在安装指南中说明

#### 6.2.2 VAD 模块 (vad.py)

- 类：`VADProcessor`
- 模型：`silero-vad`，ONNX 格式，体积 < 2MB
- 方法：
  - `process(audio_chunk: np.ndarray) -> Optional[SpeechSegment]`
  - 内部维护一个缓冲区，当语音段结束（静音 > 阈值）时返回 `SpeechSegment(start, end, audio_bytes)`
- 参数：
  - 静音判定阈值：-35 dBFS，持续时间 500ms
  - 语音段最小长度：0.3 秒（过滤杂音）
  - 最大单句长度：15 秒（强制切断）

#### 6.2.3 ASR 引擎 (asr_engine.py)

- 类：`ASREngine`
- 初始化：加载 Whisper 模型，可选设备 `cpu` 或 `cuda`
- 方法：
  - `transcribe(segment: SpeechSegment) -> dict`: 返回 `{"text": "...", "start": float, "end": float}`
- 队列：使用 `queue.Queue`，工作线程循环取 segment 执行识别
- 配置：`beam_size=5`, `language="en"`, 无额外提示
- 降级：若 GPU 不可用自动使用 CPU，模型 `small` 加载失败尝试 `tiny`
- **预留语言参数**：`language` 字段设计为可配置项，v1.0 固定为 `"en"`

#### 6.2.4 MT 引擎 (mt_engine.py)

- 类：`MTEngine`
- 初始化：加载 `opus-mt-en-zh`，tokenizer 和模型
- 方法：
  - `translate(en_text: str, context: List[str]) -> str`
- 上下文拼接逻辑：
  ```
  if context:
      prompt = " ".join(context[-3:]) + " </s> " + en_text
  else:
      prompt = en_text
  ```
- 使用 `model.generate` 参数：`max_length=200, num_beams=4`
- 结果清理：去除多余空格，保留中文标点
- **多语言预留**：构造函数接受 `source_lang` 和 `target_lang` 参数，v1.0 固定为 `("en", "zh")`

#### 6.2.5 翻译修正与调度器 (translator_coordinator.py)

- 类：`TranslationCoordinator`
- 职责：接收 ASR 结果，管理上下文窗口，触发翻译，比较修正，更新 State Manager
- 内部状态：
  - `context_window`: 固定大小 4 的双端队列，存储英文原文
  - `last_translation`: 上一句的首次译文
- 主要流程：
  1. 收到 ASR 结果 `{text, start, end}`
  2. 执行断句合并检测：如果与上一句间隔 < 0.3s 且无句号结尾 → 合并文本，标记"合并重译"，清除上一句的显示
  3. 否则：将当前文本加入 `context_window`
  4. 取 `context_window` 的前 3 条作为 context，当前句为 source，调用 MT
  5. 若当前句并非合并而来，且与上次翻译结果差异大（编辑距离 > 2），则标记为"上下文修正"
  6. 将最终译文、原文、时间戳、修正标记推送给 State Manager

#### 6.2.6 TTS 引擎 (tts_engine.py) — 新增

- 类：`TTSEngine`
- 初始化：检测可用 TTS 后端（优先 edge-tts → pyttsx3 → 不可用则降级）
- 方法：
  - `speak(zh_text: str, speed: float = 1.0)`: 将中文文本加入朗读队列
  - `stop()`: 停止当前朗读并清空队列
  - `set_speed(speed: float)`: 调节语速
- 队列：使用 `queue.Queue`，单工作线程串行朗读
- 打断策略：新句到达时默认打断当前朗读（可配置为排队模式）
- 降级：TTS 引擎全部不可用时，自动禁用语音播报按钮，仅保留字幕

#### 6.2.7 状态管理器 (state_manager.py)

- 单例模式，跨模块共享
- 属性：
  - `current_subtitle: str` (观察者绑定，变化时自动刷新字幕UI)
  - `status: Enum{RUNNING, STOPPED, ERROR}`
  - `history: List[TranslationRecord]`
  - `config: dict` (字体大小、透明度等)
- TranslationRecord 结构：
  ```python
  @dataclass
  class TranslationRecord:
      id: int
      start_time: float
      end_time: float
      en_text: str
      first_translation: str    # 首次译文
      final_translation: str    # 最终译文
      is_corrected: bool
      correction_count: int     # 被修正次数
  ```

#### 6.2.8 GUI 模块 (gui.py)

- 使用 `PyQt6` 实现（如打包体积敏感可改用 `tkinter`）
- 主控制面板：`QMainWindow`
- 字幕窗口：`QWidget` 设置 `Qt.WindowStaysOnTopHint | Qt.FramelessWindowHint | Qt.Tool`
- 历史记录：`QDialog` 包含 `QTableWidget`
- 所有 UI 更新通过信号槽连接 State Manager，避免跨线程直接操作

### 6.3 数据流示例

```
音频流: "Hello everyone. Today I'll talk about AI. It is changing..."
        │
VAD切句: [Seg1: "Hello everyone."], [Seg2: "Today I'll talk about AI."],
         [Seg3: "It is changing..."], ...
        │
ASR:    "Hello everyone." (0.5-1.2)
        "Today I'll talk about AI." (1.5-3.8)
        "It is changing the world." (4.0-5.5)
        │
MT(首次):  "大家好。" → 显示
          "今天我将讨论AI。" → 显示
          "它正在改变世界。" → 显示 (首次)
        │
MT(上下文修正): 收到第3句时context=["Hello everyone.", "Today I'll talk about AI.", "It is changing..."]
          → 重新翻译第3句得 "人工智能正在改变世界。" → 与 "它正在..." 不同
          → 更新字幕为 "人工智能正在改变世界。" (闪烁提示)
        │
TTS(可选):  "人工智能正在改变世界。" → 语音播报
```

### 6.4 核心数据结构汇总

```python
# 音频设备
@dataclass
class AudioDevice:
    id: int
    name: str
    type: str          # "input" | "output" | "loopback"
    channels: int
    sample_rate: int

# 语音片段
@dataclass
class SpeechSegment:
    id: int
    start_ms: float
    end_ms: float
    audio_data: bytes
    duration_ms: float

# ASR 结果
@dataclass
class ASRResult:
    segment_id: int
    en_text: str
    confidence: float
    start_ms: float
    end_ms: float

# MT 结果
@dataclass
class MTResult:
    segment_id: int
    zh_text: str
    en_text: str
    is_corrected: bool
    prev_translation: Optional[str]

# 翻译记录（持久化到历史面板）
@dataclass
class TranslationRecord:
    id: int
    start_time: float
    end_time: float
    en_text: str
    first_translation: str
    final_translation: str
    is_corrected: bool
    correction_count: int

# 应用配置
@dataclass
class AppConfig:
    font_size: int          # 18 | 24 | 32
    opacity: float          # 0.2 | 0.4 | 0.6 | 0.8
    audio_device_id: int
    source_language: str    # "en" (v1.0)
    target_language: str    # "zh" (v1.0)
    auto_correct: bool      # True
    tts_enabled: bool       # False
    tts_speed: float        # 1.0
    window_position: tuple  # (x, y)
    window_monitor: int     # 显示器索引
```

### 6.5 外部依赖与版本

| 库 | 版本 | 用途 |
|----|------|------|
| faster-whisper | >=0.10.0 | ASR |
| transformers | >=4.30.0 | MT |
| torch | >=2.0.0 | 模型推理 |
| sounddevice | >=0.4.6 | 音频捕获 |
| silero-vad | >=5.0 | VAD |
| PyQt6 | >=6.5 | GUI |
| pydub | >=0.25 | 音频文件处理 |
| pyinstaller | >=5.13 | 打包 |
| pyttsx3 | >=2.90 | TTS (方案C) |
| edge-tts | >=6.1 | TTS (方案A, Windows) |

### 6.6 线程模型

```
主线程:  GUI 事件循环
线程1:   音频捕获回调 (sounddevice 内部线程)
线程2:   VAD + ASR 处理循环
线程3:   MT 处理循环
线程4:   协调器循环 (可选，可与 MT 合并)
线程5:   TTS 朗读循环 (可选)
```

- 线程间通信：`queue.Queue` (ASR→MT)，`signal` (MT→UI)
- 线程安全：State Manager 内部使用 `QMutex` 或 `threading.Lock`

### 6.7 降级策略总表

| 故障场景 | 降级策略 | 用户提示 |
|----------|----------|----------|
| GPU 不可用 | 自动切换 CPU 推理 | "未检测到GPU，使用CPU模式" |
| small 模型加载失败 | 尝试 tiny 模型 | "标准模型加载失败，已切换轻量模型" |
| MT 模型崩溃 | 显示英文原文 | "翻译引擎异常，显示原文" |
| 音频设备断开 | 自动重连3次→提示手动选择 | "音频设备已断开，请重新选择" |
| VAD 无语音检测 | 降级为固定2秒切句 | "VAD异常，已切换固定切句模式" |
| 内存超过阈值(2.5GB) | 清空历史记录(保留最近50条) | "内存不足，已清理早期记录" |
| TTS 引擎不可用 | 禁用语音播报按钮，仅字幕 | "语音引擎不可用，已切换纯字幕模式" |
| 连续3句ASR失败 | 提示用户检查音频源 | "识别连续失败，请检查音频源是否正常" |
| 配置文件损坏 | 使用默认配置重建 | "配置文件已重置为默认值" |

---

## 7. 实现步骤与里程碑

以下步骤按**逻辑依赖关系**排序，每步有明确的**输入、输出、验证方法**。

---

### 步骤一：环境搭建与模型就绪

**目标**：确保所有依赖可安装，模型文件可正确下载与加载。

**前置条件**：Python 3.10+ 已安装，有 CUDA 环境的 GPU 可选但非必须。

**任务清单**：
1. 创建虚拟环境，安装核心依赖：`faster-whisper`, `transformers`, `torch`, `sounddevice`, `silero-vad`
2. 编写模型下载与加载测试脚本：
   - 加载 Whisper `small` 模型，转写一段示例英文音频（可录制或下载）
   - 加载 `opus-mt-en-zh` 模型，翻译一段测试文本
3. 记录模型加载时间、内存占用、推理延迟基线

**输出**：
- `requirements.txt`
- 测试脚本 `test_env.py`
- 性能基线文档（CPU/GPU 延迟）

**验证**：
- 运行 `test_env.py`，控制台输出识别和翻译结果，无明显错误
- 模型加载成功，内存占用 < 2.5GB

**预计耗时**：2-3 小时（含模型下载）

---

### 步骤二：音频捕获与 VAD 切句

**目标**：实现从系统音频或麦克风获取音频流，并正确切分为完整短句片段。

**前置条件**：步骤一完成，`sounddevice` 可用。

**任务清单**：
1. 枚举可用音频设备，区分输入/输出，识别 Loopback 设备
2. 实现 `AudioCapture` 类，启动流并写入缓冲区
3. 集成 `silero-vad`，实现 `VADProcessor`
4. 编写测试：播放一段包含对话的音频文件，检查 VAD 输出的语音段数量和边界是否合理
5. 将语音段保存为临时 WAV 文件，人工听检是否完整

**输出**：
- `audio_capture.py`
- `vad.py`
- 测试脚本 `test_audio_pipeline.py`，运行时打印切句的时间戳

**验证**：
- 播放已知文本的音频，VAD 切句边界误差 < 0.3 秒
- 噪声环境下不会误触（室内正常底噪）

**预计耗时**：3-4 小时

---

### 步骤三：ASR 引擎集成与管道联通

**目标**：将 VAD 输出的语音段送入 Whisper，输出正确英文文本，并建立初步的翻译管道。

**前置条件**：步骤二切句输出稳定。

**任务清单**：
1. 实现 `ASREngine` 类，模型加载与 `transcribe` 方法
2. 建立消息队列 `vad_queue`，VAD 产生的 segment 入队
3. ASR 工作线程从队列取任务，识别后放入 `asr_result_queue`
4. 编写测试：使用本地音频文件模拟流式输入，打印识别文本，与原始文本对比准确率

**输出**：
- `asr_engine.py`
- `pipeline.py` (VAD→ASR 管道骨架)
- 准确率测试报告（WER 词错率 < 15% 可接受）

**验证**：
- 连续处理 1 分钟音频，无崩溃，无累积延迟
- 识别结果在语义上可懂，无明显重复或遗漏

**预计耗时**：3-4 小时

---

### 步骤四：翻译引擎集成与首次字幕显示

**目标**：实现英译中，并将结果显示在简陋的 UI 上，验证端到端闭环。

**前置条件**：步骤三提供英文文本流。

**任务清单**：
1. 实现 `MTEngine` 类，单句翻译方法
2. 创建 `TranslationCoordinator` 简易版（不含修正逻辑）
3. 用 `tkinter` 或 PyQt 搭建一个极简窗口：一个 Label 显示最新翻译
4. 连接管道：ASR → Coord. → MT → UI
5. 运行真实音频流测试，观察字幕是否及时出现

**输出**：
- `mt_engine.py`
- `translator_coordinator.py` (v0.1)
- `gui.py` (仅字幕 Label)

**验证**：
- 播放英文句子后，字幕能在 3 秒内出现正确中文
- 连续句子更新流畅，无卡顿

**预计耗时**：3-4 小时

---

### 步骤五：上下文修正机制实现

**目标**：实现断句合并（Reshoot）和上下文回顾修正，并更新 UI 显示修正效果。

**前置条件**：步骤四端到端可运行。

**任务清单**：
1. 在 `TranslationCoordinator` 中实现：
   - 时间间隔检测与断句合并逻辑
   - 上下文滑动窗口管理
   - 上下文拼接翻译与结果比较
2. 完善 State Manager，存储首次/最终译文及修正标志
3. 修改字幕 UI：当收到修正事件时，执行闪烁高亮动画
4. 编写针对性测试：准备含代词、歧义名词的测试音频，验证修正行为

**输出**：
- 完善后的 `translator_coordinator.py` (v1.0)
- 修正算法说明文档
- UI 修正动画代码

**验证**：
- 播放测试音频，首次译文与修正后译文有可观察到的变化
- 历史记录中正确标记了修正项
- 无错误修正（好译文被改坏的情况）

**预计耗时**：4-5 小时

---

### 步骤六：完整 GUI 与辅助功能

**目标**：交付最终用户使用的完整界面，含控制面板、历史记录、设置、导出功能。

**前置条件**：步骤五功能稳定。

**任务清单**：
1. 使用 PyQt6 重构主控制面板（或增强 tkinter 界面）
2. 实现音频源选择下拉框，设备枚举
3. 历史记录窗口：表格展示，滚动，复制
4. 设置功能：字体大小、透明度滑块，配置持久化读写
5. 导出功能：TXT 和 SRT 生成逻辑
6. 系统托盘图标与最小化
7. UI 整体美化与响应式布局
8. 日志系统集成：日志初始化、文件写入、级别控制

**输出**：
- 完整 GUI 代码
- 日志模块代码
- 用户操作手册（简短 Markdown）

**验证**：
- 所有按钮、菜单可用，无功能性 bug
- 导出文件内容正确，SRT 可导入视频播放器验证

**预计耗时**：6-8 小时

---

### 步骤七：TTS 语音播报集成（v2.0 / 时间允许）

**目标**：实现翻译结果语音朗读功能。

**前置条件**：步骤五功能稳定。

**任务清单**：
1. 实现 `TTSEngine` 类，支持 edge-tts / pyttsx3 后端
2. 在主面板增加「语音播报」开关和语速选择
3. TTS 队列与翻译管线的对接
4. 打断策略与异常降级处理

**输出**：
- `tts_engine.py`
- UI 相关控件代码

**验证**：
- 开启语音播报后，每句翻译完成能听到中文朗读
- 关闭后立即停止朗读

**预计耗时**：2-3 小时

---

### 步骤八：系统测试、性能优化与打包

**目标**：确保软件在目标平台稳定运行，交付可执行文件。

**前置条件**：步骤六（和可选的步骤七）完成。

**任务清单**：
1. 编写测试用例：
   - 功能测试：各音频源切换、长时间运行无崩溃
   - 性能测试：测量端到端延迟、内存占用
   - 异常测试：无设备启动、设备热插拔、文件格式错误
2. 优化：
   - 模型加载后预热，避免首次识别卡顿
   - 确认 GPU 推理正确配置
   - 日志系统，记录错误便于演示时排查
3. 使用 PyInstaller 打包：
   - 包含模型文件（或提供单独模型包）
   - 测试在纯净 Windows 10/11 虚拟机运行
4. 录制演示视频：
   - 展示从启动到翻译修正的全流程
   - 演示离线能力（关闭网络）
5. 编写 README 与提交文档

**输出**：
- 可执行安装包或绿色版压缩包
- 测试报告
- 演示视频 (3~5 分钟)
- 提交用的代码仓库

**验证**：
- 在未装 Python 的电脑上能直接运行
- 视频中所有核心功能正常展示

**预计耗时**：5-6 小时

---

## 8. 测试策略

### 8.1 测试层次

| 测试类型 | 覆盖范围 | 工具 | 执行频率 |
|----------|----------|------|----------|
| 单元测试 | 各模块独立功能（VAD切句、MT翻译、修正比较） | `pytest` | 每次提交 |
| 集成测试 | VAD→ASR→MT 管道联通、TTS 集成 | `pytest` + 预录制音频 | 每次提交 |
| 性能测试 | 端到端延迟、内存占用、CPU使用率 | `psutil` + 计时埋点 | 每日/里程碑 |
| 压力测试 | 连续2小时运行、内存泄漏检测 | 循环播放长音频 | 里程碑 |
| 异常测试 | 设备热插拔、模型缺失、权限不足 | 手动场景清单 | 发布前 |
| 准确率测试 | WER（ASR）、翻译质量（人工评估） | 标准测试集 | 里程碑 |

### 8.2 测试用例清单（核心）

| 编号 | 测试项 | 输入 | 预期输出 |
|------|--------|------|----------|
| UT-01 | VAD 正常切句 | 3秒语音 + 1秒静音 + 2秒语音 | 输出2个语音段，边界误差 < 0.3s |
| UT-02 | VAD 过滤噪音 | 持续白噪声 | 无语音段输出 |
| UT-03 | ASR 识别准确率 | LibriSpeech clean 测试集 | WER < 15% |
| UT-04 | MT 翻译一致性 | 同一输入翻译3次 | 结果完全一致（确定性推理） |
| UT-05 | 断句合并 | 间隔200ms的两个片段 | 合并为一句，重新翻译 |
| UT-06 | 上下文修正 | "Apple...It is..." 音频 | 修正后译文包含"新芯片/苹果" |
| IT-01 | 管道联通 | 1分钟英文音频 | 输出中文字幕序列，无遗漏 |
| IT-02 | TTS 集成 | 翻译结果送入 TTS | 语音播放，不阻塞翻译管线 |
| PT-01 | 端到端延迟 | 计时埋点 | GPU < 3s, CPU < 6s |
| PT-02 | 内存泄漏 | 连续运行2小时 | 内存增长 < 100MB |
| ET-01 | 设备热插拔 | 运行中拔出USB麦克风 | 提示重选设备，不崩溃 |
| ET-02 | 模型缺失 | 删除模型文件后启动 | 提示下载，不崩溃 |

### 8.3 性能基准测试方法

```
端到端延迟测量：
1. 在播放音频的时间轴上标记每句话的结束时间 T_speech_end
2. 在 State Manager 中记录每句译文首次显示时间 T_display
3. 延迟 = T_display - T_speech_end
4. 取所有句子的 P50、P95、P99

内存占用测量：
1. 使用 psutil.Process().memory_info().rss 每10秒采样
2. 记录峰值和均值
3. 2小时持续运行后检查内存增长趋势
```

---

## 9. 验收标准

| 类别 | 验收项 | 标准 |
|------|--------|------|
| 功能 | 系统音频捕获 | 能获取浏览器/播放器声音，字幕跟随 |
| 功能 | 实时翻译 | 延迟在目标范围内 |
| 功能 | 上下文修正 | 至少演示一例可见的译文修正 |
| 功能 | 字幕显示 | 半透明置顶，可拖拽，可调大小 |
| 功能 | 历史记录 | 可查看所有翻译记录，含修正标记 |
| 功能 | 导出 | 成功导出 TXT 和 SRT，SRT 时间轴正确 |
| 功能 | 语音播报 | 可开关朗读，语速可调（P2） |
| 性能 | 端到端延迟 | GPU < 3s, CPU < 6s |
| 稳定性 | 长时间运行 | 连续运行 30 分钟无崩溃 |
| 非功能 | 离线运行 | 关闭网络后所有功能正常 |
| 非功能 | 隐私安全 | 无网络请求，音频不落盘，日志不含敏感数据 |
| 非功能 | 代码仓库 | 结构清晰，README 完整，依赖声明准确 |
| 非功能 | 路径约束 | 所有产出物位于项目根目录，不写入 C 盘 |

---

## 10. 风险与应急预案

| 风险 | 概率 | 影响 | 预案 |
|------|------|------|------|
| Windows Loopback 音频捕获在某些设备不可用 | 中 | 高 | 提供备选方案：使用麦克风对扬声器；或引入 VB-Cable 虚拟声卡 |
| Whisper small 模型在 CPU 上延迟过高（>5s） | 中 | 高 | 降级为 tiny 模型，牺牲一点精度；在演示机器上使用 GPU |
| silero-vad 对非英语语音段切分不佳 | 低 | 中 | 调整 VAD 参数；允许用户手动关闭 VAD，改为固定时长切句 |
| 翻译模型产生的译文不符合中文习惯 | 中 | 低 | 接受，演示时选择合适的音频内容，避免复杂俚语 |
| PyInstaller 打包后文件巨大（>3GB） | 高 | 中 | 模型文件外置，提供分离下载；或使用压缩算法 |
| 开发时间不足，某步骤阻塞 | 低 | 高 | 果断砍掉 P2 功能（文件导入、导出 SRT、TTS），保证核心闭环 |
| edge-tts 依赖微软在线服务 | 中 | 低 | 降级为 pyttsx3 离线 TTS；或标记 TTS 为 P2 功能 |

---

## 11. 附录

### 附录 A — 术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| ASR | Automatic Speech Recognition | 自动语音识别，将语音转为文字 |
| MT | Machine Translation | 机器翻译，将源语言文字转为目标语言文字 |
| VAD | Voice Activity Detection | 语音活动检测，判断音频中是否有人说话 |
| TTS | Text-to-Speech | 文本转语音，将文字合成为语音播报 |
| WER | Word Error Rate | 词错率，衡量 ASR 识别准确率的指标 |
| 端到端延迟 | End-to-End Latency | 从原句说完到中文译文显示的总时间 |
| 上下文修正 | Context-aware Correction | 利用后续上下文重新翻译之前句子的机制 |
| Loopback | Loopback | 音频环路设备，用于捕获系统播放的声音 |
| dBFS | Decibels Relative to Full Scale | 分贝满刻度，衡量音频信号强度的单位 |
| Reshoot | Reshoot | 断句重译，合并被误切的两个语音段重新翻译 |

### 附录 B — 项目目录结构

```
D:\AI同声传译助手\
├── src/                        # 源代码
│   ├── main.py                 # 应用入口
│   ├── audio_capture.py        # 音频捕获模块
│   ├── vad.py                  # 语音活动检测模块
│   ├── asr_engine.py           # 语音识别引擎
│   ├── mt_engine.py            # 机器翻译引擎
│   ├── tts_engine.py           # 语音合成引擎 (v2.0)
│   ├── translator_coordinator.py  # 翻译调度与修正
│   ├── state_manager.py        # 状态管理器
│   └── gui.py                  # 图形界面
├── models/                     # 模型文件目录（不纳入版本控制）
│   ├── whisper-small/          # Whisper small 模型 (~1.8GB)
│   ├── whisper-tiny/           # Whisper tiny 模型 (~700MB, 降级用)
│   └── opus-mt-en-zh/          # 翻译模型 (~600MB)
├── tests/                      # 测试代码
│   ├── test_env.py             # 环境与模型加载测试
│   ├── test_audio_pipeline.py  # 音频管道测试
│   ├── test_asr.py             # ASR 单元测试
│   ├── test_mt.py              # MT 单元测试
│   └── test_coordinator.py     # 协调器测试
├── logs/                       # 日志文件（自动创建，不纳入版本控制）
├── exports/                    # 导出文件默认目录
├── config.json                 # 用户配置文件
├── hotwords.json               # 热词表配置 (v2.0)
├── requirements.txt            # Python 依赖
├── README.md                   # 项目说明
└── 设计文档.md                 # 本文档
```

### 附录 C — config.json 字段定义

```json
{
  "font_size": 24,
  "opacity": 0.6,
  "audio_device_id": null,
  "audio_source": "system",
  "source_language": "en",
  "target_language": "zh",
  "auto_correct": true,
  "tts_enabled": false,
  "tts_speed": 1.0,
  "tts_interrupt": true,
  "whisper_model": "small",
  "window_position": [null, null],
  "window_size": [800, 140],
  "window_monitor": 0,
  "hotwords_enabled": false,
  "log_level": "INFO"
}
```

### 附录 D — 模型下载地址

| 模型 | 大小 | 下载地址（HuggingFace） | 镜像地址（国内） |
|------|------|--------------------------|-------------------|
| faster-whisper small | ~1.8GB | `https://huggingface.co/Systran/faster-whisper-small` | `https://hf-mirror.com/Systran/faster-whisper-small` |
| faster-whisper tiny | ~700MB | `https://huggingface.co/Systran/faster-whisper-tiny` | `https://hf-mirror.com/Systran/faster-whisper-tiny` |
| opus-mt-en-zh | ~600MB | `https://huggingface.co/Helsinki-NLP/opus-mt-en-zh` | `https://hf-mirror.com/Helsinki-NLP/opus-mt-en-zh` |
| silero-vad | ~2MB | `https://github.com/snakers4/silero-vad` | 自动下载 |

### 附录 E — 代码规范约定

- Python 版本：3.10+
- 代码风格：PEP 8
- 类型注解：推荐使用 Type Hints
- 命名规范：
  - 类名：PascalCase（如 `AudioCapture`）
  - 函数/变量：snake_case（如 `start_capture`）
  - 常量：UPPER_SNAKE（如 `MAX_SENTENCE_LENGTH`）
- 文档字符串：Google style docstring
- 线程安全：所有跨线程共享的状态使用 `threading.Lock` 保护

### 附录 F — 版本演进路线图

| 版本 | 核心功能 | 预计时间 |
|------|----------|----------|
| v1.0 | 英→中翻译 + 字幕 + 修正 + GUI | 3天（本次交付） |
| v1.1 | 日志系统完善、降级策略、异常处理增强 | +1天 |
| v2.0 | TTS 语音播报、热词表、多显示器记忆 | +2天 |
| v2.1 | 多语言支持（日/韩/法/德）、文件模式暂停恢复 | +3天 |
| v3.0 | 说话人识别、术语库自定义、翻译风格选择 | 远期 |

---

> **文档结束** · 所有项目产出物与配置文件均存放于 `D:\AI同声传译助手\` 目录下。
