# AI 同声传译助手 · 软件说明文档

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-06-07 | 最终版，完整用户手册 |

---

## 一、产品概述

**AI 同声传译助手**（AI Simultaneous Interpretation Assistant）是一款完全离线运行的桌面端实时英译中工具。它能够实时捕获系统音频输出或麦克风输入，将英文语音流逐句识别并翻译成中文，以可拖拽、半透明的中英双语悬浮字幕形式呈现在屏幕前端，同时具备基于上下文的自动修正能力。

### 1.1 产品定位

本工具面向需要实时理解英文内容的用户群体，典型场景包括：

1. **观看英文视频**：在浏览器或播放器中播放 YouTube、TED 演讲、技术分享等英文视频时，悬浮字幕实时显示中英双语翻译。
2. **跨国线上会议**：捕获 Zoom、Microsoft Teams 等会议软件的系统音频，实时翻译讨论内容，不遗漏要点。
3. **线下课堂与讲座**：将麦克风靠近音源，拾取英文授课或演讲的环境音，辅助理解。
4. **英语学习**：中英对照显示原文与译文，帮助理解真实语速的英文内容。

### 1.2 核心特性

- **完全离线**：所有模型本地运行，不发起任何网络请求。音频数据仅在内存中暂存处理，不落盘持久化。日志文件不含原始音频数据和完整翻译文本。
- **实时翻译**：端到端延迟约 600-750ms（tiny 模型 + CPU 模式），紧跟说话人节奏。采用异步管线架构，语音识别和翻译在独立线程中并行处理。
- **中英双字幕**：悬浮窗口同时显示英文原文（上排，较小字体）和中文译文（下排，较大字体），统一金色字体，透明背景，一目了然。
- **上下文自修正**：内置 Reshoot 断句合并算法，当语音检测模块误将一句话切成两段时，自动检测相邻片段的时间间隔和标点特征，合并英文原文后重新翻译，用正确译文替换错误显示。修正时字幕短暂闪烁金色提示用户。
- **自适应语音检测**：纯 numpy 实现的能量检测 VAD，自动追踪环境噪声水平并动态调整灵敏度。在安静办公室和嘈杂环境下均能正常工作。
- **字幕调节**：字体大小支持 24px、32px、40px 三档切换，文字透明度支持 20%-80% 滑块调节，设置实时生效并持久化保存。
- **字幕导出**：支持导出 TXT 格式（纯文本，每行一句中文，带时间戳）和 SRT 格式（标准字幕，含序号、时间轴、中文文本），可使用视频播放器加载。
- **历史记录**：表格展示所有翻译记录（时间、英文原文、中文译文、修正标记），被自动修正的句子以黄色背景高亮显示，支持复制单条或全部内容。
- **系统托盘**：最小化到系统托盘，托盘图标常驻，双击恢复主面板，右键菜单提供快捷操作。

### 1.3 隐私与安全

本工具严格遵守隐私优先原则：
- 所有语音处理完全在本地完成，不调用任何云端 API。
- 音频数据仅在内存中暂存，不写入磁盘持久化。
- 日志文件仅记录运行状态和错误信息，不包含原始音频数据。
- 导出文件默认存放于项目目录内 `exports/` 文件夹，不写入系统敏感目录。
- 配置文件和模型缓存全部限定在项目根目录内。

---

## 二、系统要求

### 2.1 操作系统

| 操作系统 | 最低版本 | 说明 |
|----------|----------|------|
| Windows 10 | 1809+ | 推荐，完整支持 WASAPI Loopback |
| Windows 11 | 所有版本 | 推荐 |
| macOS | 12.0+ | 需配合 BlackHole 虚拟声卡实现系统音频捕获 |

### 2.2 硬件配置

| 项目 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | Intel i5 第 8 代或 AMD Ryzen 5 | Intel i7 第 10 代或更高 |
| 内存 | 8 GB | 16 GB |
| 显卡 | 无需独立显卡 | NVIDIA GTX 1060 6GB+（可选 GPU 加速） |
| 磁盘空间 | ~3 GB（含模型文件） | ~5 GB（含所有可选模型） |
| Python 版本 | 3.10+ | 3.11 或 3.12 |

### 2.3 依赖软件

- Python 3.10 或更高版本
- 依赖包详见项目根目录 `requirements.txt`

---

## 三、安装指南

### 3.1 获取项目

```bash
# 克隆仓库
git clone https://github.com/Limerence-mj/AI-Simultaneous-Interpretation-Assistant.git
cd AI-Simultaneous-Interpretation-Assistant
```

### 3.2 安装 Python 依赖

```bash
pip install -r requirements.txt
```

主要依赖包括：
- `faster-whisper`：语音识别引擎
- `transformers` + `torch`：翻译模型加载与推理
- `sounddevice`：麦克风音频捕获
- `numpy`：音频数据处理与 VAD 实现
- `PyQt6`：图形用户界面
- `pydub`：音频文件格式处理

### 3.3 下载模型文件

```bash
python scripts/download_models.py
```

此脚本将下载以下模型到 `models/` 目录：

| 模型 | 大小 | 用途 |
|------|------|------|
| faster-whisper-small | ~1.8 GB | 英文语音识别（主力引擎） |
| faster-whisper-tiny | ~700 MB | 英文语音识别（降级备选） |
| opus-mt-en-zh | ~600 MB | 英→中机器翻译 |
| sherpa-zipformer-en | ~200 MB | 流式语音识别（备选引擎） |
| HY-MT1.5-1.8B-FP8 | ~1.5 GB | 翻译引擎备选（精度更高，可选下载） |

### 3.4 验证安装

```bash
python tests/test_env.py
```

验证脚本将检查：
- Python 版本是否满足 3.10+
- 所有核心依赖是否可正常导入
- GPU 是否可用及型号检测
- Whisper small 模型能否正常加载与推理
- opus-mt-en-zh 模型能否正常加载与翻译
- 音频设备是否可枚举
- 当前内存占用是否在合理范围内

### 3.5 首次运行

```bash
python src/main.py
```

程序启动后将自动：
1. 初始化日志系统（日志文件写入 `logs/` 目录）
2. 加载用户配置（`config.json`，不存在则自动创建默认配置）
3. 检查模型文件完整性
4. 枚举可用音频设备
5. 显示主控制面板 GUI

---

## 四、使用指南

### 4.1 界面概览

程序启动后显示主控制面板，包含两个标签页：

**控制页**：
- 音频来源下拉框（系统音频 / 麦克风）
- 音频设备下拉框（自动枚举并标注设备类型）
- 设备刷新按钮
- 开始翻译 / 停止翻译按钮
- 实时状态指示器（就绪 / 加载中 / 运行中 / 错误）
- 统计信息行（已翻译句数、修正次数、平均延迟、运行时长）

**设置页**：
- 字幕字体大小下拉框（24px / 32px / 40px）
- 字幕透明度滑块（20% ~ 80%）
- 自动修正复选框（默认开启）
- 设置实时生效，关闭程序后自动保存

主面板底部辅助按钮：📋 历史记录、💾 导出字幕、📺 显示/隐藏字幕窗口。

### 4.2 操作步骤

**第一步：选择音频源**

在控制页「音频来源」下拉框中选择：
- 「系统音频」：捕获电脑正在播放的声音（浏览器、播放器、会议软件等）
- 「麦克风」：捕获物理麦克风输入

选择后点击右侧 ⟳ 按钮刷新设备列表，在下拉框中选择具体设备。设备列表标注设备类型图标（🔊 系统音频、🎤 麦克风）。

**第二步：开始翻译**

点击 **▶ 开始翻译** 按钮。状态指示变为黄色并显示「正在加载模型」。Whisper 语音识别模型和 opus-mt 翻译模型在后台线程中加载（约 10-15 秒），加载期间界面不卡顿。

加载完成后状态变为绿色并显示「运行中」。音频捕获自动开始，无需额外操作。

**第三步：播放英文音频/视频**

打开任意英文视频或音频文件开始播放。翻译结果将实时显示在字幕窗口和主面板状态栏中。中英双语字幕同时更新。

**第四步：调节字幕显示**

点击 **📺 字幕** 按钮打开悬浮字幕窗口。如需调整字幕外观，切换到「设置」标签页：
- 选择字体大小（24px / 32px / 40px）
- 拖动透明度滑块（20% ~ 80%，数值越大文字越亮）
- 修改实时生效

字幕窗口可在屏幕上任意拖拽，关闭后重新打开会恢复上次位置。

**第五步：查看历史与导出**

点击 **📋 历史** 打开历史记录窗口，可滚动查看所有翻译记录。被自动修正的句子以黄色背景高亮显示。支持：
- 复制选中行到剪贴板
- 复制全部内容到剪贴板
- 查看翻译统计数据（总句数、修正次数）

点击 **💾 导出** 将翻译记录保存为文件：
- TXT 格式：`exports/export_YYYY-MM-DD_HH-MM-SS.txt`
- SRT 格式：`exports/export_YYYY-MM-DD_HH-MM-SS.srt`（标准字幕，含时间轴）

**第六步：停止翻译**

点击 **⏹ 停止** 结束翻译。音频捕获停止，翻译管线安全关闭，等待队列中的未处理结果完成。

### 4.3 系统托盘

程序支持最小化到系统托盘。关闭主窗口或点击最小化按钮后，托盘图标常驻。
- 双击托盘图标：显示主面板
- 右键托盘图标：显示菜单（显示主面板、显示/隐藏字幕、退出）

点击退出将停止翻译管线、保存当前配置、关闭所有窗口。

### 4.4 配置持久化

程序关闭时自动保存以下设置到 `config.json`：
- 字幕字体大小
- 字幕透明度
- 音频来源（系统音频/麦克风）
- 自动修正开关

下次启动时自动读取并应用这些设置。

---

## 五、功能详解

### 5.1 音频捕获

本工具支持两种音频捕获模式，通过 `AudioCapture` 类统一封装。

**系统音频模式（WASAPI Loopback）**

技术实现：纯 ctypes 调用 Windows Core Audio API（WASAPI），不使用第三方音频库。

工作流程：
1. 通过 COM 接口创建 `MMDeviceEnumerator` 实例
2. 获取默认音频渲染端点（eRender, eConsole）
3. 激活 `IAudioClient` 接口并配置为 Loopback 模式
4. 获取混音格式（通常为 48000Hz 立体声 32-bit float）
5. 在后台线程中持续读取音频帧（100ms 周期）
6. 多声道平均降混为单声道
7. 线性插值重采样至 16000Hz
8. 按 1024 帧块回调给翻译管线

**麦克风模式**

技术实现：使用 `sounddevice` 库的 `InputStream`。

工作流程：
1. 枚举 sounddevice 输入设备（MME、DirectSound、WASAPI）
2. 验证设备可用性（尝试以 16000Hz / 1ch / float32 打开流）
3. 启动输入流，回调函数接收音频块
4. 如设备采样率与管线不匹配，在回调中进行重采样
5. 多声道自动降混为单声道

### 5.2 语音活动检测（VAD）

本工具采用自研的纯 numpy 能量 VAD，完全替代 silero-vad，消除对 torch/torchaudio 的依赖。

**为什么要自研 VAD？**

原计划使用 silero-vad（基于 ONNX 的先进语音检测模型），但在实际部署中发现：silero-vad 依赖 torch → torchaudio，而用户环境中的 `libtorchaudio.pyd` 存在 DLL 依赖缺失问题，导致整个翻译管线无法启动。修复该问题需要管理员权限重装 PyTorch，对用户体验不友好。因此自研纯 numpy 方案，零外部依赖，兼容性完美。

**算法原理**

1. **噪声底自适应估计**：使用指数加权移动平均（EMA）持续追踪环境背景噪声水平。初始值设定为 0.002（中等安静环境）。平滑系数 0.95，仅在不处于语音状态时更新，避免语音信号污染噪声估计。

2. **动态阈值计算**：每次处理音频块时，根据当前噪声底计算两个阈值：
   - 语音进入阈值 = 噪声底 × 3.5（高门槛，防止噪音误触）
   - 语音退出阈值 = 噪声底 × 1.3（低门槛，迟滞效应防止句中断裂）

3. **计时器机制**：
   - 语音确认：音频能量连续 ≥ 进入阈值 200ms → 标记为语音开始
   - 静音切句：音频能量连续 < 退出阈值 250ms → 标记为语音结束，提取语音段
   - 最长安全网：连续语音 ≥ 3 秒 → 强制切断，防止超长句导致后续处理延迟
   - 最短过滤：语音段 < 200ms → 丢弃为噪音碎片

4. **音频缓冲区**：保留最近 15 秒原始音频数据，当语音段确认后从缓冲区提取对应范围的 PCM 16-bit 字节数据，用于后续 ASR 处理。

**参数调整**

以上参数为默认值，经过多轮测试在安静办公室和一般家庭环境中表现良好。如需在极安静或极嘈杂环境使用，可调整 `src/vad.py` 中的参数。

### 5.3 语音识别（ASR）

**引擎配置**

- 模型：faster-whisper（基于 CTranslate2 的 Whisper 加速实现）
- 默认模型：small（约 1.8GB，平衡精度与速度）
- 备选模型：tiny（约 700MB，加载快 2 倍，推理快 3 倍）
- 计算设备：CPU（int8 量化）或 GPU（float16）
- CPU 线程数：4（推理并行度）

**解码参数**

| 参数 | 值 | 说明 |
|------|-----|------|
| beam_size | 1 | 贪婪解码，只选最优路径，速度最大化 |
| best_of | 1 | 仅生成 1 个候选，不进行多候选比较 |
| temperature | 0.0 | 确定性输出，相同输入产生相同结果 |
| language | "en" | 固定英文识别 |
| vad_filter | False | 已由外部 VAD 完成切句，不重复处理 |
| condition_on_previous_text | False | 每句独立识别，避免跨句幻觉传播 |
| compression_ratio_threshold | 2.4 | 检测音频压缩失真，过滤异常输入 |
| log_prob_threshold | -1.0 | 过滤低置信度输出 |
| no_speech_threshold | 0.6 | 检测非语音段 |

**性能数据**（CPU Intel i7，small 模型，int8 量化）：
- 模型加载时间：约 6 秒
- 单段推理延迟：100ms ~ 500ms（取决于语音段长度，1-3 秒段典型 200ms）
- 内存占用：约 1.8 GB（模型加载后）

### 5.4 机器翻译（MT）

**引擎配置**

- 模型：Helsinki-NLP/opus-mt-en-zh
- 架构：MarianMT（Transformer Encoder-Decoder）
- 参数规模：约 74M
- 推理延迟：单句 < 100ms（CPU）

**翻译参数**

| 参数 | 值 | 说明 |
|------|-----|------|
| max_length | 动态计算 | 输入长度 × 3，范围 [20, 200] |
| num_beams | 4 | Beam Search 宽度 |
| early_stopping | True | 所有 beam 生成 EOS 即停止 |
| repetition_penalty | 1.2 | 抑制重复 token 生成 |
| no_repeat_ngram_size | 3 | 禁止 3-gram 重复出现 |

**后处理**

1. 空白规范化：合并多余空格，统一空白字符
2. 重复折叠（`_collapse_repetitions`）：
   - 词级去重：相同单词只保留一次，允许长词（>2 字）重复
   - 重复率检测：去重后剩余不足原词数 40% 时，判定为循环输出，截取前半段
   - 字符级去重：连续相同 CJK（中日韩）字符去重

### 5.5 上下文修正

**Reshoot 断句合并**

场景：VAD 将自然说话中的短暂停顿（如 "I think... the most important thing"）误判为句尾，导致一句话被切成两段。

触发条件：
1. 当前句与前一句在时间线上间隔 < 300ms
2. 前一句不以句号、感叹号、问号结尾

处理流程：
1. 合并：将前一句的英文原文与当前句的英文原文用空格拼接
2. 重译：以合并后的完整文本调用 MT 引擎重新翻译
3. 替换：用新译文替换前一翻译记录
4. 提示：字幕文字短暂闪烁亮金色（800ms），告知用户译文已修正

上下文回顾翻译（Context-aware Re-translation）接口已保留但当前版本未启用，因为启用后 opus-mt 的上下文拼接翻译导致输出重复率上升，译文质量反而下降。

### 5.6 异步翻译管线

本工具采用双队列异步架构，将语音识别和机器翻译解耦为独立线程。

**ASR 层**：音频块 → VAD 切句 → SpeechSegment → ASR Queue（容量 10）→ ASR Worker 线程读取 → faster-whisper 转写 → ASRResult → 放入 MT Queue

**MT 层**：MT Worker 线程从 MT Queue（容量 50）读取 → TranslationCoordinator 处理（过滤低质量结果、Reshoot 检测、翻译、状态更新）→ StateManager 更新字幕

**关键设计决策**：
- ASR 和 MT 各自独立线程，互不阻塞。ASR 持续处理音频不受翻译速度影响。
- MT Queue 容量 50，防止翻译积压导致内存溢出。超出容量时丢弃最旧条目。
- 停止时 MT Worker 等待队列清空（最长 30 秒），确保所有已识别内容完成翻译。
- `queue.Queue` 天然线程安全，无需额外同步。

### 5.7 翻译质量过滤

在 TranslationCoordinator 中，翻译结果进入字幕显示之前经过多级过滤：

1. 空文本检测：ASR 输出为空或标记为错误 → 跳过
2. 置信度过滤：ASR 置信度 < 0.5 → 跳过（过滤背景噪音和低质量识别）
3. 长度过滤：英文原文去标点后 < 5 个字符 → 跳过
4. 单词数过滤：英文原文单词数 < 2 个 → 跳过（过滤单字噪音）

这些过滤机制有效消除了"you"、"yes"、"well" 等背景噪音导致的无效翻译。

---

## 六、界面与交互

### 6.1 主控制面板

主控制面板采用暗色现代风格（深灰底 + 蓝白文字），包含两个标签页和底部辅助按钮栏。

**控制标签页**包含音频源选择、开始/停止按钮、状态指示、统计信息。

**设置标签页**包含字幕字体大小、透明度、自动修正开关。所有设置实时生效，关闭程序时自动保存。

### 6.2 悬浮字幕窗口

字幕窗口设计为独立于主面板的悬浮窗，具有以下特征：
- 无边框，背景完全透明
- 窗口置顶（`WindowStaysOnTopHint`）
- 可拖拽移动（鼠标左键按住拖动）
- 双行显示：英文原文在上（字号为中文的 75%），中文译文在下（100% 字号）
- 统一金色文字 `rgb(255, 220, 50)`，在深色背景上对比度高且不刺眼
- 修正时文字短暂变亮金色闪烁（800ms 后恢复）
- 窗口尺寸 1100 × 320 像素
- 右键菜单提供字体大小、透明度快捷调节和退出选项
- 30fps 定时器轮询字幕状态，增量刷新

### 6.3 历史记录窗口

独立的对话框窗口，表格包含 5 列：时间、英文原文、首次译文、最终译文、修正标记。
- 修正行黄色背景高亮
- 底部显示统计：总句数、修正次数
- 支持复制选中行或全部内容到剪贴板

---

## 七、配置说明

### 7.1 配置文件路径

`config.json` 位于项目根目录。文件不存在时自动创建并填充默认值。文件损坏时自动回退到默认配置。

### 7.2 配置项说明

```json
{
  "font_size": 24,              // 字幕字体大小 (24/32/40)
  "opacity": 0.6,               // 文字透明度 (0.2 ~ 0.8)
  "audio_device_id": null,      // 上次使用的音频设备 ID
  "audio_source": "system",     // 音频来源 ("system" | "microphone")
  "source_language": "en",      // 源语言
  "target_language": "zh",      // 目标语言
  "auto_correct": true,         // 是否启用自动修正
  "whisper_model": "small",     // ASR 模型大小 ("small" | "tiny")
  "window_position": [null, null], // 字幕窗口位置 [x, y]
  "window_size": [800, 140],    // 字幕窗口尺寸 [width, height]
  "window_monitor": 0,           // 显示字幕的显示器索引
  "log_level": "INFO"           // 日志级别
}
```

---

## 八、日志系统

- 日志文件路径：`logs/app_YYYY-MM-DD.log`（按日期自动命名）
- 日志格式：`YYYY-MM-DD HH:MM:SS [LEVEL] module: message`
- 日志级别：DEBUG / INFO / WARNING / ERROR
- 保留策略：自动清理 7 天前的旧日志
- 控制台同步输出（INFO 及以上级别）
- 隐私保护：不记录原始音频数据和完整翻译文本

---

## 九、项目目录结构

```
AI-Simultaneous-Interpretation-Assistant/
├── src/                            # 源代码（20 个 Python 文件）
│   ├── main.py                     # 应用入口，环境初始化
│   ├── audio_capture.py            # 音频捕获（WASAPI Loopback + sounddevice）
│   ├── vad.py                      # VAD 模块（纯 numpy 能量检测）
│   ├── asr_engine.py               # ASR 引擎（faster-whisper）
│   ├── asr_engine_streaming.py     # 流式 ASR 引擎（Sherpa-ONNX，备选扩展）
│   ├── mt_engine.py                # MT 引擎（opus-mt-en-zh）
│   ├── pipeline.py                 # 管线调度器（VAD→ASR→MT 异步队列）
│   ├── pipeline_streaming.py       # 流式管线（备选扩展）
│   ├── translator_coordinator.py   # 翻译协调器（Reshoot + 状态更新）
│   ├── state_manager.py            # 状态管理器（线程安全单例）
│   ├── config_manager.py           # 配置文件读写
│   ├── gui.py                      # 图形界面（PyQt5/6，约 1200 行）
│   ├── logger.py                   # 日志系统
│   ├── export_utils.py             # 字幕导出（TXT/SRT）
│   ├── tts_engine.py               # 离线 TTS（pyttsx3，扩展接口）
│   ├── tts_engine_edge.py          # 在线 TTS（Edge-TTS，扩展接口）
│   ├── asr_groq.py                 # Groq ASR API（扩展接口）
│   ├── mt_groq.py                  # Groq MT API（扩展接口）
│   └── pipeline_api.py             # Groq API 管线（扩展接口）
├── models/                         # 模型文件目录
│   ├── models--Systran--faster-whisper-small/  # ~1.8GB
│   ├── models--Systran--faster-whisper-tiny/   # ~700MB
│   ├── opus-mt-en-zh/              # ~600MB
│   ├── sherpa_zipformer_en/        # ~200MB（备选）
│   └── hy-mt1.5/                   # ~1.5GB（备选）
├── tests/                          # 测试代码（7 个测试文件）
│   ├── test_env.py                 # 环境与模型加载验证
│   ├── test_audio_pipeline.py      # 音频管道测试
│   ├── test_asr_pipeline.py        # ASR 管道测试
│   ├── test_mt_pipeline.py         # MT 管道测试
│   ├── test_correction.py          # 修正算法测试
│   ├── test_step6.py               # 端到端集成测试
│   └── test_tts.py                 # TTS 可用性测试
├── scripts/                        # 工具脚本
│   └── download_models.py          # 模型下载脚本（5 步下载，支持增量跳过）
├── test_media/                     # 测试视频
│   ├── ted_talk.mp4                # 短 TED 演讲（~27MB）
│   └── TED_Vulnerability.mp4       # 长 TED 演讲（~324MB）
├── logs/                           # 运行日志（按天滚动）
├── exports/                        # 导出字幕文件
├── config.json                     # 用户配置文件
├── requirements.txt                # Python 依赖声明
├── README.md                       # 项目说明
└── 软件说明文档.md                  # 本文档
```

---

## 十、扩展功能说明

以下功能模块代码已保留在项目中，作为扩展接口，当前版本未集成到主界面的使用流程中。

### 10.1 语音播报（TTS）

代码位置：`src/tts_engine.py`（离线，基于 pyttsx3 调用 Windows SAPI5 语音合成）、`src/tts_engine_edge.py`（在线，基于 Edge-TTS 调用微软云端高质量语音合成）。

如需启用：在 `src/gui.py` 的 `_on_init_finished` 方法中，将 `self._tts_engine = None` 替换为 TTS 引擎的初始化代码。GUI 中 TTS 相关的控件（复选框、音色选择、语速选择）已从设置页面移除，如需恢复需重新添加 UI 控件。

### 10.2 Groq API 在线加速

代码位置：`src/asr_groq.py`（Groq Whisper API）、`src/mt_groq.py`（Groq Llama 3 API）、`src/pipeline_api.py`（完整 Groq 管线）。

如需启用：在 `src/gui.py` 中恢复 API 模式的 UI 控件和代码路径。使用 Groq API 可以显著降低延迟（云端 GPU 推理）并提升翻译质量（Llama 大模型），但需要联网和 API Key。

### 10.3 流式 Sherpa ASR

代码位置：`src/asr_engine_streaming.py`（Zipformer 实时流式识别器）、`src/pipeline_streaming.py`（流式管线）。

Sherpa-ONNX 的 Zipformer 模型理论上可实现 < 500ms 的流式识别延迟。但在实际测试中，该模型对系统 Loopback 音频的识别效果不佳（无法可靠检测语音端点），因此当前版使用 VAD + faster-whisper 组合作为主力管线。

### 10.4 HY-MT1.5 翻译引擎

模型位置：`models/hy-mt1.5/`（腾讯混元翻译模型，~1.5GB）。

该模型翻译质量显著优于 opus-mt，但 CPU 推理时存在 FP8 量化权重的逐层解压开销，单句翻译延迟高达 4-12 秒，不适合实时同传场景。已下载的模型文件保留供未来 GPU 推理使用。

---

## 十一、常见问题解答

**Q1：启动后点开始翻译，长时间无反应？**

模型首次加载需要约 10-15 秒（Whisper small ~6s + opus-mt ~1s）。请等待状态指示从 🟡 变为 🟢。如果超过 2 分钟仍未就绪，查看 `logs/` 目录下的日志文件排查错误。

**Q2：翻译开始后没有字幕出现？**

请确认：
1. 系统音频正在播放（非静音状态）
2. 音频设备选择正确（系统音频模式需使用 🔊 标注的设备）
3. 已点击 📺 字幕按钮打开字幕窗口
4. 日志中 `[音频]` 消息的能量值 > 0（能量连续为 0 说明未捕获到音频）

**Q3：翻译延迟感觉很大？**

当前 CPU 模式端到端延迟约 600-750ms。如需更低延迟，可修改 `src/gui.py` 中 `PipelineInitWorker` 的 `model_size="tiny"` 切换快速模型（延迟降至约 400ms，但识别准确率略有下降）。

**Q4：翻译质量不如预期？**

opus-mt 是轻量级翻译模型（74M 参数），适合实时场景，但译文偏直译。如需更高质量，可使用 Groq API 模式（联网）或启用 HY-MT1.5（需 GPU）。

**Q5：字幕颜色、大小如何调整？**

切换到「设置」标签页，使用字体下拉框和透明度滑块调节。字幕窗口打开后右键菜单也可快速调节。设置自动保存，下次启动生效。

**Q6：配置损坏了怎么办？**

删除项目根目录的 `config.json` 文件，重新启动程序将自动创建默认配置。

**Q7：字幕窗口不见了？**

点击主面板的 📺 字幕按钮重新打开。如窗口不小心被拖到屏幕外，删除 `config.json` 中的 `window_position` 配置可恢复默认位置。

**Q8：如何添加麦克风输入？**

在控制页「音频来源」下拉框中选择「麦克风」，点击 ⟳ 刷新，在下拉列表中选择对应设备。

**Q9：程序占用太多内存？**

Whisper small 模型加载后约占用 1.8GB 内存，加上翻译模型约 2.5GB。如需降低内存占用，可改用 tiny 模型（约 1GB 总占用）。

---

> **文档版本**: v1.0 | **日期**: 2026-06-07
>
> 项目仓库：https://github.com/Limerence-mj/AI-Simultaneous-Interpretation-Assistant
>
> 许可证：MIT
>
> 所依赖的第三方模型和库各按其原始许可证分发。
