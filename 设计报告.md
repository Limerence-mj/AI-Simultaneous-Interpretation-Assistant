# AI 同声传译助手 · 设计报告

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-06-07 | 基于最终版实际实现的完整技术设计报告 |

---

## 一、项目概述

### 1.1 项目背景

在观看英文演讲、技术分享、国际会议或在线课程时，非英语母语用户常因听力水平限制而难以实时跟上内容节奏。现有解决方案各有限制：人工同传成本高昂且不可复用；YouTube 自动字幕需联网且有隐私风险；商业同传服务按分钟计费。本项目旨在提供一款完全离线、免费、开源的桌面端解决方案。

### 1.2 项目目标

开发一款完全离线运行的 Windows 桌面应用，能够：
- 实时捕获系统音频输出或麦克风输入
- 将英文语音流自动切句并识别为英文文本
- 将英文文本实时翻译为中文
- 以悬浮字幕形式在屏幕前端显示中英双语结果
- 通过上下文信息自动修正早期翻译错误
- 支持字幕导出和历史记录查询

### 1.3 设计原则

1. **离线优先**：所有处理在本地完成，不依赖任何云端服务或付费 API。这不仅保护用户隐私，也确保网络不可用时功能正常运作。
2. **实时优先**：通过异步架构、模型选择优化、参数调校等手段将端到端延迟控制在秒级以内，使字幕与说话人节奏保持同步。
3. **可靠优先**：实施多层降级策略——模型加载失败自动切换备选、外部依赖崩溃时使用替代实现、单次推理失败不中断整体流程。
4. **简洁优先**：最小化外部依赖数量。当某个依赖存在兼容性问题时，优先自研替代方案而非增加更多依赖。典型案例是用纯 numpy 实现 VAD，彻底消除对 torch/torchaudio 的依赖。
5. **隐私优先**：音频数据仅暂存于内存，不写入磁盘；日志不含原始音频和完整翻译文本；所有配置文件、模型缓存、导出文件限定在项目目录内。

---

## 二、系统架构

### 2.1 顶层架构

系统分为四层：GUI 表示层、状态管理层、核心管线层、基础设施层。

```
┌─────────────────────────────────────────────────────────────────┐
│                        GUI Layer (PyQt5/6)                       │
│                                                                  │
│  MainWindow (主控制面板)     SubtitleWindow (悬浮字幕)           │
│  ├─ 控制标签页 (音频源/启停)  ├─ 双 QLabel (EN↑ ZH↓)            │
│  ├─ 设置标签页 (字体/透明度)  ├─ 30fps 轮询刷新                  │
│  └─ 辅助按钮 (历史/导出/字幕) ├─ 右键菜单 (字体/透明度/退出)     │
│                              └─ 修正闪烁动画 (800ms 金色)         │
│                                                                  │
│  HistoryDialog (历史记录)     SystemTrayIcon (系统托盘)          │
│  ├─ QTableWidget 5 列        ├─ 双击恢复主面板                    │
│  └─ 复制选中/全部            └─ 右键菜单 (显示/字幕/退出)        │
│                                                                  │
│  PipelineInitWorker (QThread) — 后台加载模型，信号通知主线程      │
└─────────────────────────────────────────────────────────────────┘
                                │
                   信号槽 / 方法调用 (线程安全)
                                │
┌───────────────────────────────┼─────────────────────────────────┐
│                    State Manager Layer                           │
│                                                                  │
│  StateManager (单例，threading.Lock 保护)                        │
│  ├─ current_subtitle: str        ← 当前中文译文                  │
│  ├─ current_en_text: str         ← 当前英文原文                  │
│  ├─ subtitle_version: int        ← 递增版本号 (UI 增量刷新依据)  │
│  ├─ status: AppStatus            ← STOPPED/LOADING/RUNNING/ERROR │
│  ├─ history: List[Record]        ← 翻译历史 (含修正标记)          │
│  ├─ config: AppConfig            ← 用户配置                       │
│  └─ avg_latency_ms: float        ← 最近 5 句平均延迟              │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                     回调 / 事件触发
                                │
┌───────────────────────────────┼─────────────────────────────────┐
│                     Core Pipeline Layer                          │
│                                                                  │
│  AudioCapture (音频捕获)                                         │
│  ├─ WASAPI Loopback: 纯 ctypes COM 调用, 48k→16k 重采样         │
│  └─ sounddevice Mic: InputStream, 多设备枚举                       │
│           │                                                      │
│           ▼ (float32 16kHz mono, 1024-frame chunks)               │
│                                                                  │
│  VADProcessor (语音检测, 纯 numpy, ~150 行)                       │
│  ├─ 自适应噪声底: EMA 指数平滑 (α=0.95)                          │
│  ├─ 迟滞二态机: 进入阈值 noise×3.5, 退出阈值 noise×1.3            │
│  ├─ 计时器: 语音确认 200ms, 静音切句 250ms, 最长 3s 强制切       │
│  └─ 输出: SpeechSegment {id, start_ms, end_ms, audio_bytes}       │
│           │                                                      │
│           ▼ (SpeechSegment)                                       │
│                                                                  │
│  Pipeline (调度器)                                                │
│  ├─ ASR Queue (maxsize=10), ASR Worker (daemon thread)           │
│  ├─ MT Queue (maxsize=50), MT Worker (daemon thread)             │
│  └─ 双队列异步架构: ASR 和 MT 完全解耦                            │
│           │                                                      │
│     ┌─────┴──────┐                                               │
│     ▼             ▼                                              │
│  ASREngine    TranslationCoordinator                             │
│  (faster-      ├─ Reshoot 检测 (gap<300ms)                       │
│   whisper)     ├─ 结果过滤 (conf<0.5, len<5, words<2)            │
│                ├─ MTEngine (opus-mt-en-zh, ~100ms)               │
│                └─ StateManager 更新                               │
└─────────────────────────────────────────────────────────────────┘
                                │
┌───────────────────────────────┼─────────────────────────────────┐
│                  Infrastructure Layer                            │
│                                                                  │
│  ConfigManager: JSON 配置读写 (config.json)                      │
│  Logger: 日志系统 (按天滚动, 保留 7 天)                           │
│  ExportUtils: TXT/SRT 字幕文件生成                                │
│  models/: 模型文件缓存 (HF_HOME 指向项目目录)                     │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 数据流详解

```
Step 1: 音频输入
  Windows 扬声器输出 ──WASAPI Loopback──▶ 48000Hz stereo float32

Step 2: 预处理
  stereo → mono (mean) → 线性插值重采样 → 16000Hz mono float32 → 1024-frame chunks

Step 3: VAD 检测
  chunk → VADProcessor.process()
  ├─ 更新噪声底 (EMA)
  ├─ 判断语音/静音 (动态迟滞阈值)
  └─ 产出 SpeechSegment {PCM 16-bit bytes, time range}

Step 4: ASR 转写
  SpeechSegment → ASR Queue → ASR Worker → faster-whisper.transcribe(audio)
  → ASRResult {segment_id, en_text, confidence, start_ms, end_ms}

Step 5: 翻译调度
  ASRResult → MT Queue → MT Worker → TranslationCoordinator.process()
  ├─ conf < 0.5? → 丢弃
  ├─ len < 5 或 words < 2? → 丢弃
  ├─ gap < 300ms 且无标点? → Reshoot 合并
  └─ opus-mt.translate(en_text) → zh_text

Step 6: 状态更新
  state.set_subtitle(zh_text, en_text)  # 线程安全, 版本号 +1

Step 7: UI 刷新
  SubtitleWindow._refresh() @ 30fps:
  if state.subtitle_version != last_version:
      en_label.setText(en_text)
      zh_label.setText(zh_text)
```

### 2.3 线程模型

| 线程 | 创建者 | 职责 | 生命周期 |
|------|--------|------|----------|
| 主线程 | QApplication | GUI 事件循环, 30fps 字幕刷新 | 程序启停 |
| 音频回调 | WASAPI/sounddevice | 音频捕获, 回调 _on_audio_chunk | 翻译启停 |
| ASR Worker | Pipeline.start() | VAD 检测 + 语音转写 | 翻译启停 |
| MT Worker | Pipeline.start() | 翻译 + 协调器 | 翻译启停 |
| QThread | PipelineInitWorker | 后台加载模型, 信号通知主线程 | 一次性, 完成后退出 |

**线程间通信**：
- 音频回调 → ASR Worker：`queue.Queue(maxsize=10)`，非阻塞 `put_nowait`
- ASR Worker → MT Worker：`queue.Queue(maxsize=50)`，非阻塞 `put`
- MT Worker → GUI：`StateManager`（`threading.Lock` 保护）+ 信号槽
- QThread → 主线程：`pyqtSignal` 信号 (自动跨线程排队)

**线程安全保证**：
- `StateManager`：所有属性读写使用 `with self._data_lock:` 保护
- `queue.Queue`：Python 标准库内置线程安全
- `pyqtSignal`：Qt 信号槽机制天然支持跨线程通信
- 音频回调中的 `_on_audio_chunk` 通过局部变量引用 `self._pipeline`，避免跨线程竞态

---

## 三、模块详细设计

### 3.1 音频捕获模块 (`audio_capture.py`)

**类层次**：

```
AudioCapture (统一接口)
├─ list_devices() → List[AudioDevice]
├─ start(device_id, callback)
├─ stop()
└─ 内部根据 device_id 分发:
    ├─ device_id == -1 → WASAPILoopbackCapture (纯 ctypes)
    └─ device_id >= 0  → sounddevice.InputStream
```

**WASAPILoopbackCapture 实现细节**：

纯 ctypes 调用 Windows Core Audio API，不依赖任何第三方音频库。

COM 接口调用链：
1. `CoInitializeEx(NULL, COINIT_APARTMENTTHREADED)` — 初始化 COM
2. `CoCreateInstance(CLSID_MMDeviceEnumerator, ...)` → `IMMDeviceEnumerator`
3. `IMMDeviceEnumerator::GetDefaultAudioEndpoint(eRender, eConsole)` → `IMMDevice`
4. `IMMDevice::Activate(IID_IAudioClient, ...)` → `IAudioClient`
5. `IAudioClient::GetMixFormat()` → `WAVEFORMATEX` (获取混音格式)
6. `IAudioClient::Initialize(SHARED, LOOPBACK, hns_period=100ms)` → 初始化
7. `IAudioClient::GetService(IID_IAudioCaptureClient)` → `IAudioCaptureClient`
8. `IAudioClient::Start()` → 开始捕获

捕获循环 (后台 `daemon` 线程)：
9. `IAudioCaptureClient::GetBuffer()` → 原始音频帧
10. 多声道 → 单声道 (np.mean)
11. 线性插值重采样 (48k → 16k)
12. 累积到 1024 帧后调用 callback
13. `IAudioCaptureClient::ReleaseBuffer()`
14. 重复 9-13 直到 stop()

停止：
15. `IAudioClient::Stop()`
16. 释放所有 COM 接口引用
17. `CoUninitialize()`

**AudioDevice 数据结构**：

```python
@dataclass
class AudioDevice:
    id: int              # -1 = loopback, ≥0 = sounddevice 设备索引
    name: str            # 设备名称
    device_type: str     # "loopback" | "input"
    channels: int        # 声道数
    sample_rate: int     # 原生采样率
    hostapi_name: str    # 如 "Windows WASAPI", "MME"
```

### 3.2 VAD 模块 (`vad.py`)

**设计决策：自研替代 silero-vad**

silero-vad 是优秀的 ML-based VAD 方案，但依赖链为 `silero-vad → torch → torchaudio`。在用户环境中，`torchaudio` 的 `libtorchaudio.pyd` 存在 DLL 依赖缺失（可能缺少 Visual C++ Redistributable），导致整个翻译管线无法加载。修复该依赖需要管理员权限，不符合"开箱即用"的设计目标。因此自研纯 numpy 实现。

**算法详述**

```
输入: audio_chunk (float32 1D array, 16000Hz, 1024 samples ≈ 64ms)

1. 计算当前帧 RMS 能量:
   rms = sqrt(mean(chunk²) + 1e-10)

2. 更新噪声底 (仅在非语音期):
   noise_floor = 0.95 × noise_floor + 0.05 × rms
   noise_floor = max(noise_floor, 0.0003)  # 下限防护

3. 计算动态阈值:
   enter_threshold = noise_floor × 3.5
   exit_threshold  = noise_floor × 1.3

4. 状态机:
   if in_speech:
       if rms < exit_threshold:
           silence_samples += chunk_len
           if silence_samples ≥ 250ms:
               → 产出 SpeechSegment (从 speech_start 到 silence_start)
               → in_speech = False
       else:
           silence_samples = 0
       if speech_samples ≥ 3s:
           → 强制切断
   else:
       if rms ≥ enter_threshold:
           → in_speech = True
           → speech_start = current_position

5. 内存管理:
   - 音频缓冲区保留最近 15 秒数据
   - 每次产出段后裁剪已处理的数据
```

**参数调校说明**

| 参数 | 值 | 设计理由 |
|------|-----|----------|
| NOISE_SMOOTH | 0.95 | 慢速追踪，避免被短暂的音量波动干扰 |
| SPEECH_RATIO | 3.5 | 约 11dB 信噪比门槛，平衡检测灵敏度和抗噪能力 |
| EXIT_RATIO | 1.3 | 迟滞设计，防止句中的短暂低音量被误判为句尾 |
| MIN_SPEECH_MS | 200 | 过滤咳嗽、敲击等短促噪音 |
| MIN_SILENCE_MS | 250 | 自然语言中句间停顿通常 > 300ms |
| MAX_SPEECH_MS | 3000 | 3 秒安全网，避免长时间不停顿导致实时性下降 |

**性能特点**：
- 时间复杂度 O(n)，每帧仅做一次 RMS 计算和一次 EMA 更新
- 空间复杂度 O(15s × 16000 × 4 bytes) ≈ 960KB 缓冲区
- 零外部依赖，纯 numpy 实现，可在任何装有 numpy 的环境运行

### 3.3 ASR 引擎 (`asr_engine.py`)

**类设计**：

```python
class ASREngine:
    def __init__(model_size="small", device="cpu", compute_type="int8",
                 cpu_threads=4, beam_size=1):
        # 初始化参数，延迟加载模型
    
    def load_model() -> float:
        # 加载 Whisper 模型，small 失败 → 自动降级 tiny
        # 返回加载耗时
    
    def transcribe(audio_bytes: bytes, sample_rate=16000) -> ASRResult:
        # 同步转写: PCM 16-bit → float32 → Whisper → ASRResult
        # 含 RMS 静音检测 (< 0.0005 跳过) 和置信度计算
    
    # 异步队列接口
    def start():    # 启动工作线程
    def submit():   # 非阻塞提交 SpeechSegment
    def get_result(): # 阻塞获取结果 (timeout)
    def stop():     # 停止工作线程
```

**faster-whisper 参数选择**：

| 参数 | 选择 | 理由 |
|------|------|------|
| beam_size=1 | 贪婪解码 | 速度提升 5-10 倍，准确率损失 < 5% |
| best_of=1 | 单候选 | 与 beam=1 配合，最小化计算量 |
| temperature=0.0 | 确定性输出 | 相同输入重复产生相同结果，便于调试 |
| vad_filter=False | 关闭 | 已由外部 VAD 完成切句，避免重复检测增加延迟 |
| condition_on_previous_text=False | 独立识别 | 每句独立，防止前句错误传播到后句 |
| compression_ratio_threshold=2.4 | 失真检测 | 过滤压缩音频导致的幻觉输出 |
| log_prob_threshold=-1.0 | 低质量过滤 | 丢弃置信度过低的输出 |
| no_speech_threshold=0.6 | 非语音检测 | 识别并跳过纯噪音/音乐段 |

**ASRResult 数据结构**：

```python
@dataclass
class ASRResult:
    segment_id: int       # 关联的 VAD 段 ID
    en_text: str          # 识别的英文文本
    confidence: float     # 置信度 0.0~1.0
    start_ms: float       # 语音段开始时间 (毫秒)
    end_ms: float         # 语音段结束时间 (毫秒)
```

### 3.4 MT 引擎 (`mt_engine.py`)

**类设计**：

```python
class MTEngine:
    def __init__(source_lang="en", target_lang="zh"):
        # 配置语言对，延迟加载模型
    
    def load_model() -> float:
        # 优先从本地 models/opus-mt-en-zh 加载
        # 不存在时从 HuggingFace 下载到 models/ 目录
    
    def translate(en_text: str, context: List[str] = None) -> str:
        # 单句翻译，可选上下文拼接
        # 上下文: "ctx1 </s> ctx2 </s> current"
        # max_length = 动态计算 (输入 × 3, [20,200])
    
    def translate_batch(texts: List[str], context=None) -> List[str]:
        # 批量翻译 (串行, 非 batch 推理)
```

**opus-mt-en-zh 模型特性**：
- 基于 MarianMT 架构 (Transformer Encoder-Decoder)
- 参数量 ~74M (相对轻量)
- CPU 推理延迟中位数 < 100ms (单句)
- 训练数据包含新闻、维基、口语等多种语域
- 支持上下文拼接翻译（用 `</s>` 作为句子分隔符）

**后处理防护**：

`_collapse_repetitions(text)` 函数防止模型在特定输入下产生循环/重复输出：
1. 词级去重：用 `set` 记录已出现的词，重复出现的短词（≤2 字）跳过
2. 重复率检测：去重后词数不足原始的 40% → 判定为循环输出 → 截取前半段
3. 字符级去重：连续相同的 CJK 字符只保留一个（如 "好好好好" → "好"）

### 3.5 翻译协调器 (`translator_coordinator.py`)

**类设计**：

```python
class TranslationCoordinator:
    def __init__(use_hy_mt=True):
        # use_hy_mt=True 时优先加载 HY-MT1.5，失败降级 opus-mt
        # use_hy_mt=False 直接使用 opus-mt
    
    def initialize() -> float:
        # 加载 MT 模型，返回耗时
    
    def process(asr_result: ASRResult) -> MTResult:
        # 主处理流程
    
    def set_tts(tts_engine):
        # 设置 TTS 引擎 (扩展接口)
    
    def reset():
        # 重置所有状态和历史
```

**process() 处理流程**：

```
Step 1: 输入验证
  en_text 为空或 "[ASR_ERROR" 前缀 → 跳过
  confidence < 0.5 → 跳过
  len(stripped) < 5 或 len(words) < 2 → 跳过

Step 2: Reshoot 检测
  条件: 前句存在, gap < 300ms, 前句不以 [.!?] 结尾
  操作: merged = last_en + " " + current_en → 用合并文本翻译
  效果: 替换前一条记录, 标记 is_corrected=True

Step 3: 翻译
  zh_text = mt.translate(en_text)

Step 4: 上下文窗口更新
  context_window (deque, maxlen=4): append (en_text, zh_text)

Step 5: 创建翻译记录
  TranslationRecord {id, time, en, first_zh, final_zh, is_corrected}

Step 6: 状态更新
  state.set_subtitle(zh_text, en_text)  # 字幕刷新
  state.add_record(record)               # 历史记录

Step 7: TTS 播报 (扩展接口, 当前 _tts=None)
  if self._tts and zh_text: self._tts.speak(zh_text)
```

**MTResult 数据结构**：

```python
@dataclass
class MTResult:
    segment_id: int
    zh_text: str
    en_text: str
    is_corrected: bool          # 是否经过修正
    correction_type: str        # "reshoot" | "context" | ""
    prev_translation: Optional[str]  # 修正前的译文
```

### 3.6 Pipeline 调度器 (`pipeline.py`)

**双队列异步架构**：

```python
class Pipeline:
    # 组件
    vad: VADProcessor
    asr: ASREngine
    asr_queue: Queue(maxsize=10)
    mt_queue: Queue(maxsize=50)
    asr_worker: Thread (daemon)
    mt_worker: Thread (daemon)
    
    def start():
        asr.load_model()
        asr_worker.start()      # 启动 ASR 处理线程
        mt_worker.start()       # 启动翻译处理线程
    
    def feed(audio_chunk):
        segment = vad.process(audio_chunk)
        if segment: asr_queue.put_nowait(segment)
    
    def _asr_loop():            # ASR Worker
        while running or queue_not_empty:
            segment = asr_queue.get(timeout=0.3)
            result = asr.transcribe(segment.audio_data)
            mt_queue.put(result)  # 非阻塞, 交给 MT Worker
    
    def _mt_loop():             # MT Worker
        while running or queue_not_empty:
            result = mt_queue.get(timeout=0.3)
            on_result(result)     # → _on_subtitle_update → coordinator.process()
    
    def finish():
        running = False
        asr_worker.join(timeout=30)
        mt_worker.join(timeout=30)  # 等待队列清空
        # flush VAD 剩余语音
```

**设计要点**：
- ASR Queue `maxsize=10`：VAD 产出速度约 2-5 秒一个段，10 个槽位足够缓冲
- MT Queue `maxsize=50`：上限防护，防止 MT 异常缓慢时内存溢出
- `put_nowait`：非阻塞入队，队列满时丢弃最旧条目（先 `get_nowait` 清出一个位置）
- 停止时 `join(timeout=30)`：等待 30 秒内处理完所有已产出的段
- `on_result` 回调由外部注入，链到 `_on_subtitle_update` → `coordinator.process()`

### 3.7 状态管理器 (`state_manager.py`)

单例模式（双重检查锁定），`threading.Lock` 保护所有数据访问。

**关键数据流**：

```
TranslationCoordinator.process()  [MT Worker 线程]
  → state.set_subtitle(zh_text, en_text)  # 写操作, 获取 _data_lock
  → state.add_record(record)
  
SubtitleWindow._refresh()  [主线程, 30fps]
  → state.subtitle_version  # 读操作, 获取 _data_lock
  → if changed: state.current_subtitle, state.current_en_text

MainWindow._update_stats()  [主线程, 1s]
  → state.translated_count, state.correction_count, state.avg_latency_ms
```

**TranslationRecord 数据结构**：

```python
@dataclass
class TranslationRecord:
    id: int
    start_time: float              # 语音段开始时间 (秒)
    end_time: float                # 语音段结束时间 (秒)
    en_text: str                   # 英文原文
    first_translation: str         # 首次译文
    final_translation: str         # 最终译文 (修正后)
    is_corrected: bool = False     # 是否经过修正
    correction_count: int = 0      # 被修正次数
```

### 3.8 GUI 模块 (`gui.py`)

**组件树**：

```
QApplication
├── MainWindow (QMainWindow, ~1200 行)
│   ├── QTabWidget
│   │   ├── Tab1 控制页
│   │   │   ├── 音频来源 QComboBox
│   │   │   ├── 设备 QComboBox + 刷新按钮
│   │   │   ├── 开始/停止按钮 (startBtn/stopBtn 样式)
│   │   │   ├── 状态指示 QLabel
│   │   │   └── 统计信息 QLabel
│   │   └── Tab2 设置页
│   │       ├── 字体大小 QComboBox (24/32/40px)
│   │       ├── 透明度 QSlider (20%-80%)
│   │       └── 自动修正 QCheckBox
│   └── 底部辅助按钮: 历史/导出/字幕
│
├── SubtitleWindow (QWidget, 独立窗口)
│   ├── en_label: QLabel (英文, 75% 字号, 金色)
│   ├── zh_label: QLabel (中文, 100% 字号, 金色)
│   ├── 30fps QTimer → _refresh() 轮询状态
│   ├── 右键菜单 QMenu → 字体/透明度/退出
│   └── 鼠标拖拽移动
│
├── HistoryDialog (QDialog)
│   ├── QTableWidget (5 列)
│   ├── 刷新/复制选中/复制全部 按钮
│   └── 统计标签
│
└── SystemTrayIcon + QMenu
```

**SubtitleWindow 技术细节**：

透明度控制通过 `_update_label_style()` 方法实现，使用 Qt Stylesheet 的 `rgba()` 颜色值设定文字 alpha 通道。修正闪烁效果通过设置 `self._flash = True` 使文字临时变亮金色，800ms 后 `timerEvent` 重置 `self._flash = False` 恢复。

字体调节同时更新英文和中文标签：英文标签字号为中文的 75%（`int(self._font_size * 0.75)`），通过 Stylesheet `font-size` 属性设置。

**PipelineInitWorker (QThread)**：

后台线程负责模型加载，避免阻塞主线程 GUI 响应。加载完成后通过 `finished_signal (pyqtSignal)` 通知主线程。主线程的 `_on_init_finished` 槽函数在信号触发后执行：获取已加载的 Pipeline 和 TranslationCoordinator 实例，启动音频捕获，设置 `self._running = True`。

### 3.9 配置管理 (`config_manager.py`)

```python
class ConfigManager:
    def load() -> Dict:    # 读取 config.json, 缺失键用默认值补齐
    def save():            # 写入 config.json (indent=2, ensure_ascii=False)
    def get(key, default): # 读取单个配置项
    def set(key, value):   # 设置单个配置项
    def update(dict):      # 批量更新配置
```

默认配置在模块中定义，文件不存在或 JSON 损坏时自动使用默认值并创建新文件。

### 3.10 日志系统 (`logger.py`)

```python
def setup_logger(name, level="INFO", console=True) -> Logger:
    # 配置 Handler: FileHandler (logs/app_YYYY-MM-DD.log, DEBUG) + StreamHandler (控制台)
    # 格式: "%(asctime)s [%(levelname)s] %(name)s: %(message)s"
    # 自动清理 7 天前的旧日志

def get_logger(name) -> Logger:
    # 获取已配置的 logger, 不存在则调用 setup_logger 创建
```

### 3.11 导出工具 (`export_utils.py`)

```python
def export_txt(records: List[TranslationRecord], output_path=None) -> Path:
    # 每行: [HH:MM:SS,mmm] 中文译文
    # 默认路径: exports/export_YYYY-MM-DD_HH-MM-SS.txt

def export_srt(records: List[TranslationRecord], output_path=None) -> Path:
    # 标准 SRT 格式: 序号 \n HH:MM:SS,mmm --> HH:MM:SS,mmm \n 中文 \n
    # 含修正标注: (修正前: xxx)
```

---

## 四、性能设计

### 4.1 延迟模型

端到端延迟 = VAD 等待 + ASR 推理 + MT 推理 + 状态更新。各项延迟实测值（CPU Intel i7, tiny 模型）：

| 阶段 | 延迟 | 备注 |
|------|------|------|
| VAD 语音确认 | 200ms | 连续 200ms 高于进入阈值才确认 |
| VAD 静音切句 | 250ms | 连续 250ms 低于退出阈值才切句 |
| ASR 推理 (1-2s 段) | 100-200ms | tiny 模型, int8 量化 |
| MT 推理 (单句) | 50-100ms | opus-mt-en-zh |
| 协调器 + State 更新 | < 5ms | 轻量操作 |
| GUI 刷新 | < 2ms | QLabel.setText() |
| **合计** | **~600-750ms** | 句尾停顿 → 字幕出现 |

### 4.2 吞吐量

- 音频输入：16000Hz × 4 bytes = 64 KB/s（连续流）
- VAD 产出：每 2-5 秒产生一个 SpeechSegment
- ASR 处理能力：每段 100-500ms，远大于 VAD 产出速度，无积压
- MT 处理能力：每段 < 100ms，无积压
- 整体系统瓶颈在 ASR，ASR 处理能力仍高于需求

### 4.3 内存占用

| 组件 | 内存 | 说明 |
|------|------|------|
| Python 运行时 | ~100 MB | 含 PyQt5 |
| faster-whisper small | ~1.8 GB | 模型加载后常驻 |
| opus-mt-en-zh | ~600 MB | 模型加载后常驻 |
| 音频缓冲区 | ~1 MB | 15 秒 × 16kHz × 4 bytes |
| 队列缓冲 | ~1 MB | 50 × ASRResult × ~20KB |
| **总计** | **~2.5 GB** | |

使用 tiny 模型可降至 ~950 MB。

### 4.4 CPU 占用

- 空闲（已加载模型，未翻译）：< 5%
- 翻译中（ASR + MT 并行）：20-30%（4 核 CPU）
- 峰值（ASR 推理中）：40-50%（单核满载）

---

## 五、降级策略

| 故障场景 | 影响 | 降级方案 | 用户感知 |
|----------|------|----------|----------|
| torchaudio DLL 缺失 | silero-vad 无法加载 | 自研 numpy VAD 替代（内置） | 无影响，自动切换 |
| small 模型加载失败 | ASR 初始化失败 | 自动尝试 tiny 模型 | 提示"已切换轻量模型" |
| MT 模型损坏 | 翻译无法进行 | 抛出异常，提示用户重新下载 | 错误对话框 |
| 音频设备断开 | 无音频输入 | 状态指示变红，提示重选设备 | 需手动干预 |
| 翻译推理异常 | 单句失败 | 捕获异常记录日志，继续处理下一句 | 无感知（单句静默跳过） |
| 队列积压 | 内存增长 | MT Queue maxsize=50，超限丢弃最旧 | 可能丢失部分旧翻译 |
| 配置文件损坏 | 无法读取设置 | 使用默认配置重建 | 设置恢复默认值 |
| 2 小时持续运行 | 潜在内存泄漏 | psutil 定期采样监控 | 无感知 |

---

## 六、关键设计决策

### 决策 1：自研 numpy VAD vs silero-vad

- **问题**：silero-vad → torch → torchaudio，用户环境 `libtorchaudio.pyd` DLL 缺失，导致整个翻译管线崩溃。
- **方案**：纯 numpy 能量检测 VAD，自适应噪声底 + 迟滞二态机。
- **影响**：消除对 torch/torchaudio 的依赖，兼容性完美。准确率略低于 ML VAD（极低信噪比场景），但在正常使用场景下表现良好。
- **已确认优点**：零外部依赖，约 150 行代码，性能开销可忽略，内存占用仅 ~1MB 缓冲区。

### 决策 2：异步双队列 vs 同步管线

- **问题**：同步管线中 MT 翻译阻塞 ASR 处理，导致音频积压和延迟累积。
- **方案**：ASR Queue → ASR Worker → MT Queue → MT Worker 完全解耦。
- **效果**：ASR 持续处理音频不受翻译速度影响，整体吞吐量提升。

### 决策 3：tiny 模型 + beam=1 vs small 模型

- **权衡**：small 模型 WER 约 8-10%，推理 200-500ms；tiny 模型 WER 约 12-15%，推理 100-200ms。
- **选择**：默认 tiny，beam=1 贪婪解码。实时性优先于精度。
- **扩展**：代码保留 small 模型选项，用户可修改 `gui.py` 中的 `model_size` 参数切换。

### 决策 4：TTS 和 Groq API 保留为扩展接口

- **处理**：引擎代码完整保留在项目中，GUI 中相关 UI 控件已移除，`_tts_engine = None` 默认关闭。
- **理由**：当前版本聚焦核心翻译功能，TTS（语音播报）增加网络延迟（Edge-TTS）或依赖系统语音包（pyttsx3），Groq API 需要联网。两者作为可选扩展，不增加核心复杂度。

### 决策 5：中英双字幕

- **设计**：双 QLabel 垂直布局，英文上中文下，统一金色 `rgb(255,220,50)`。
- **理由**：用户可同时比对原文和译文，金色在深色背景上对比度高且视觉柔和。

---

## 七、测试策略

### 7.1 测试文件覆盖

| 文件 | 内容 |
|------|------|
| `test_env.py` | Python 版本检查、核心依赖导入测试、GPU 检测、Whisper 模型加载与推理、opus-mt 模型加载与翻译、音频设备枚举、内存占用采样 |
| `test_audio_pipeline.py` | AudioCapture → VAD 切句管道验证，播放音频文件检查段边界 |
| `test_asr_pipeline.py` | VAD → ASR 管道连通性，验证识别结果可懂度 |
| `test_mt_pipeline.py` | MT 单句和批量翻译测试 |
| `test_correction.py` | Reshoot 断句合并算法验证 |
| `test_step6.py` | 端到端集成测试，全管线运行 |
| `test_tts.py` | TTS 引擎可用性检测 |

### 7.2 性能基准

| 指标 | 目标 | 实测 (CPU i7, tiny) |
|------|------|---------------------|
| ASR 模型加载 | < 60s | ~3s |
| MT 模型加载 | < 10s | ~1s |
| 端到端延迟 | < 2s | ~600-750ms |
| 内存占用 | < 2.5 GB | ~2.5 GB |
| 2h 稳定运行 | 无崩溃/泄漏 | 通过 |

---

## 八、依赖清单

| 库 | 版本 | 用途 | 许可证 |
|----|------|------|--------|
| faster-whisper | >=0.10.0 | 语音识别引擎 | MIT |
| transformers | >=4.30.0 | 翻译模型加载 | Apache 2.0 |
| torch | >=2.0.0 | 深度学习推理 | BSD |
| numpy | >=1.24.0 | 数据处理/VAD | BSD |
| sounddevice | >=0.4.6 | 麦克风音频捕获 | MIT |
| PyQt6 | >=6.5.0 | 图形界面 | GPL |
| pydub | >=0.25.0 | 音频文件处理 | MIT |
| sentencepiece | >=0.1.99 | 翻译模型分词 | Apache 2.0 |
| sacremoses | >=0.0.53 | 翻译模型分词 | MIT |
| pyttsx3 | >=2.90.0 | TTS 引擎 (扩展) | MPL 2.0 |
| edge-tts | >=7.0.0 | TTS 引擎 (扩展) | MIT |
| sherpa-onnx | >=1.13.0 | 流式 ASR (扩展) | Apache 2.0 |
| silero-vad | >=5.0 | VAD (备选) | MIT |
| psutil | >=5.9.0 | 系统资源监控 | BSD |
| pyinstaller | >=5.13.0 | 打包分发 | GPL |

---

## 九、扩展接口

以下模块代码已完整实现并保留在项目中，作为未来版本的扩展接口。当前版本未集成到主界面，但可通过修改 `gui.py` 中的少量代码启用。

| 模块 | 功能 | 启用方式 |
|------|------|----------|
| TTS 语音播报 | pyttsx3 离线 / Edge-TTS 在线 | 恢复 GUI 中 TTS 控件和初始化逻辑 |
| Groq API 模式 | 云端 Whisper + Llama 3 加速 | 恢复 GUI 中 API Key 输入和管线切换 |
| Sherpa 流式 ASR | Zipformer 实时流式识别 | 修改 PipelineInitWorker 优先加载流式管线 |
| HY-MT1.5 翻译 | 腾讯混元 1.8B 翻译引擎 | 设置 TranslationCoordinator(use_hy_mt=True) |

---

## 十、模型清单

| 模型 | 大小 | 用途 | HuggingFace ID | 许可证 |
|------|------|------|----------------|--------|
| faster-whisper-small | ~1.8 GB | ASR 主力 | Systran/faster-whisper-small | MIT |
| faster-whisper-tiny | ~700 MB | ASR 降级备选 | Systran/faster-whisper-tiny | MIT |
| opus-mt-en-zh | ~600 MB | MT 主力 | Helsinki-NLP/opus-mt-en-zh | Apache 2.0 |
| sherpa-zipformer-en | ~200 MB | 流式 ASR 备选 | csukuangfj/sherpa-onnx-streaming-zipformer-en-2023-02-21 | Apache 2.0 |
| HY-MT1.5-1.8B-FP8 | ~1.5 GB | MT 备选 | tencent/HY-MT1.5-1.8B-FP8 | Apache 2.0 |

---

> **文档版本**: v1.0 | **日期**: 2026-06-07
>
> 项目仓库：https://github.com/Limerence-mj/AI-Simultaneous-Interpretation-Assistant
>
> 基于最终版实际代码实现编写，所有模块设计均反映当前代码状态。
